# TOP_K=130 の設計意図をドキュメントに追記

## Context

改定影響調査の `evaluation.top_k: 130` の設計意図がドキュメントに記載されておらず、引き継ぎ後に「なぜ130か」「変えてよいか」が判断できない。コード調査で TOP_K=130 はバッチ処理で正しく機能していることを確認済み。ただし **エリア単位で** 130件制限がかかるため、複数エリアの改定（例: ③=4エリア）では最大 130×4=520件/プロバイダー になる点も未記載。

## 変更対象

### 1. REVISION_OPS.md（L92、設定値テーブルの直後）

追記内容:
- TOP_K=130 の設計意図（再現率重視、閾値モードが不安定だった経緯）
- 130件の根拠（Stage 1+2 のOR結合から上位を取る、コレクション規模の約半数）
- **エリア単位での制限**である旨（複数エリア時は 130×エリア数）

**ファイル**: `rag-local/docs/REVISION_OPS.md`
**挿入位置**: L92（`---` の直前）

### 2. CONFIGURATION.md（L289、top_k 設定一覧テーブルの直後）

追記内容:
- 評価用が大きい理由の一言補足（再現率重視 + エリア単位で適用）
- 詳細は REVISION_OPS.md へのリンク

**ファイル**: `rag-local/docs/CONFIGURATION.md`
**挿入位置**: L290（`---` の直前）

## 追記しない内容

- コード変更なし
- 他のドキュメントへの変更なし

## 検証

- 追記後に REVISION_OPS.md と CONFIGURATION.md を読み、テーブルとの整合性を目視確認
- 既存のリンク参照が壊れていないことを確認
