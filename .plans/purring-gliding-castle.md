# ドキュメント修正計画（第2弾: レビュー指摘事項の修正）

## Context

前回の修正（`b585cf2`）で用語統一・専門用語平易化（43箇所）を完了。その後、議事録・引き継ぎ資料・プロジェクトノートとドキュメントを突き合わせたレビューで23件の不正確・不足を発見。本計画はそのうち修正対象の項目を実施する。

### スコープ制約
- **差分ベクトル化（自動差分DB更新）はスコープ外**: rag-maintenance（クラウド環境）で実装済み、次回引き継ぎ（3/10）で説明予定。rag-localドキュメントには記載しない
- よって「運用効率化AIの2機能目が欠落」(旧#2)は対象外。rag-localの範囲では「改定影響調査」で正しい
- Teams連携の将来構想（旧#20）もスコープ外

---

## 修正一覧

### A. REVISION_OPS.md（最大の修正量）

#### A-1: 冒頭説明がシナリオのみと読める → FAQ（履歴データ）も検索可能 【重大】
- **現在** L1-3: 「事務改定前のシナリオデータを…影響を受けるシナリオを正しく見つけられるかを評価するシステム」
- **修正**: 「シナリオデータおよび問い合わせ履歴データ（FAQ）」に拡張。冒頭の1文を書き換え
- **根拠**: ops_ui.py の影響調査モードで `source_filter` により `history_data` も検索対象にできる

#### A-2: ops_ui.py（Streamlit UI）の説明が完全に欠落 【重要】
- **現在**: REVISION_OPS.md にはバッチ版（run_eval.py）の説明のみ。UIの存在はREADME.mdのテーブルに1行あるだけ
- **修正**: 「Streamlit UI（ops_ui.py）」セクションを追加
  - 2モード構成: 評価モード（正解ID付き精度検証）、影響調査モード（正解IDなし、実運用向け影響範囲調査）
  - 影響調査モードの特徴: データソース選択（シナリオ / FAQ）、業務エリア選択、検索タイプ選択（hybrid / keyword_filter）
  - 起動コマンド: `streamlit run apps/revision-ops/ui/ops_ui.py`
- **挿入位置**: 「## 使用方法」セクション内、Step 2 の後あたり、または独立セクション

#### A-3: 「評価するシステム」という表現 → 実運用ツールとしても使える 【中程度】
- **現在** L3: 「影響を受けるシナリオを正しく見つけられるかを**評価する**システム」
- **修正**: 評価に加えて影響調査（実運用）にも使える旨を記載。A-1と合わせて冒頭を修正

### B. README.md

#### B-1: テーブルの用途列がシナリオのみ 【重大】
- **現在** L10: `事務改定で影響を受けるシナリオ候補を調査`
- **修正**: `事務改定で影響を受けるシナリオや問い合わせ履歴の候補を調査`

#### B-2: ディレクトリコメント 【軽微】
- **現在** L199: `# 運用保守効率化AI（改定影響調査）`、L202: `# 改定影響調査 Streamlit UI`
- **修正**: L202 を `# 改定影響調査 Streamlit UI（評価モード + 影響調査モード）` に変更

#### B-3: 「2つのAIアプリケーション」に問い合わせ整理AIの言及なし 【軽微】
- **現在** L5: 「2つのAIアプリケーションと共有コアで構成する」
- **修正**: 文末に補足追加「※ 問い合わせ整理AIは別リポジトリ（rag-maintenance）で管理」程度の注釈

### C. ANSWER_SUPPORT.md

#### C-1: 業務背景・導入経緯の記載がない 【重要】
- **修正**: 「## 概要」の前または中に2-3行の背景を追加
  - 「問い合わせ対応チームが受ける質問に対し、過去の問い合わせ履歴やシナリオボットのデータから類似回答を提示し、回答作成を支援するシステム」
  - 精度実績: 回答提示率 48% → 67%（ハイブリッド検索導入後）

#### C-2: 「なぜoriginalがデフォルトか」の経緯がない 【重要】
- **修正**: search_mode テーブル（L49-53周辺）の近くにNote追加
  - 「original がデフォルトの理由: LLM拡張（llm_enhanced）は要約により固有名詞が落ちるケースがあり、原文検索の方が精度が高い場合が多いため」

#### C-3: 「質問と回答の両方をベクトル化」の設計判断理由 【重要】
- **修正**: DB構造セクションのあたりにNote追加
  - 「質問文と回答文の両方を結合してベクトル化している理由: 質問文だけでは同じ趣旨でも表現が異なるケースをカバーしきれず、回答文も含めることで意味的な一致精度が向上するため」

### D. CONFIGURATION.md

#### D-1: search_source テーブルにops_uiでのFAQ検索が説明されていない 【中程度】
- **現在** L380-393: search_source は回答支援AIの文脈のみで説明
- **修正**: ops_ui.py（影響調査モード）でも `source_filter` としてシナリオ/FAQを切替可能であることを追記
  - 「**改定影響調査UI（影響調査モード）でも同様のデータソース切替が可能です**」

### E. ARCHITECTURE.md

#### E-1: ops_ui.py の2モード構造が未記載 【中程度】
- **現在**: ARCHITECTURE.md にはバッチ版（run_eval.py → MultiStageOrchestrator）の処理フローのみ
- **修正**: ops_ui.py のエントリーポイント説明に「評価モード / 影響調査モードの2モード構成」を追記。詳細はREVISION_OPS.mdに委譲

### F. TROUBLESHOOTING.md

#### F-1: 修正なし
- レビューで挙がった項目（#6-B, #6-C等）は前回の修正で対処済み。追加修正不要

### G. CLAUDE.md

#### G-1: faqコメントが古い「FAQデータ」のまま 【軽微】
- **現在** L255: `faq/ # FAQデータ`
- **修正**: `faq/ # 問い合わせ履歴データ（FAQ）` に統一（前回修正で他ドキュメントは対応済みだがCLAUDE.mdは漏れ）

---

## 修正の進め方

| 順番 | ファイル | 修正箇所数 | 主な修正 |
|------|---------|-----------|---------|
| 1 | REVISION_OPS.md | 3 | 冒頭修正 + ops_ui セクション追加 + 表現修正 |
| 2 | README.md | 3 | テーブル用途修正 + ディレクトリコメント + 注釈追加 |
| 3 | ANSWER_SUPPORT.md | 3 | 業務背景追加 + originalデフォルト理由 + Q&Aベクトル化理由 |
| 4 | CONFIGURATION.md | 1 | search_source にops_ui言及追加 |
| 5 | ARCHITECTURE.md | 1 | ops_ui 2モード追記 |
| 6 | CLAUDE.md | 1 | FAQコメント修正 |

合計: **12箇所**

## 検証方法

- 各ドキュメント修正後、「シナリオのみ」と読める記述が残っていないかを grep で確認
- ops_ui.py の実際の機能（2モード、source_filter）と記述の整合性を確認
- 差分ベクトル化に言及していないことを確認
