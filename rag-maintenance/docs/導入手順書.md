# Phase2 PoC Azure環墁E導�E手頁E��

**斁E��ID**: SETUP-FAQ-IMPACT-001

**版数**: 1.7

**作�E日**: 2026/2/10

**作�E老E*: チE��タル戦略部

**関連斁E��**: REQ-FAQ-IMPACT-002�E�Ehase2 PoC 要件定義書 v3.2�E�E

---

## 改訂履歴

| 版数 | 日仁E| 変更冁E�� | 拁E��E|
|------|------|---------|------|
| 1.0 | 2026/2/10 | 初版作成 | デジタル戦略部 |
| 1.1 | 2026/2/10 | セルフレビューによる10件修正: ロール付与先修正（Azure OpenAI→AI Search MI）、データソース/Indexer設計を2系統に変更、SDK選定を要件定義書と整合、Key Vaultロール付与追加、Cosmos DB管理プレーンロール追加、不要な環境変数削除、Week 3注記追加 | デジタル戦略部 |
| 1.2 | 2026/2/10 | リソース命名規則変更（impact→maintenance）、要件定義書参照をv3.2に更新 | デジタル戦略部 |
| 1.3 | 2026/2/10 | Step 8・11・12をM365 Agents Toolkit ベースに書き換え、Step 13にF5ローカルデバッグ追加 | デジタル戦略部 |
| 1.4 | 2026/2/10 | Toolkit v6系対応: `teamsapp.yml`→`m365agents.yml`リネーム、テンプレート選択フロー（Custom Engine Agent → Basic Custom Engine Agent）に修正、プロジェクト構造を実態に合わせ更新、サインイン手順に具体アカウント名を明記 | デジタル戦略部 |
| 1.5 | 2026/2/16 | FR-015 SPO連携: Step 9にGraph API権限付与（`Files.ReadWrite.All`）・SPOサイトID/ドライブID取得手順を追加、`.localConfigs`にSPO環境変数3項目を追加 | デジタル戦略部 |
| 1.6 | 2026/2/20 | Indexerスケジュールを`PT1H`→`PT10M`に変更、差分ベクトル化のコスト特性（ポーリング頻度ではなく変更ドキュメント数に依存）を追記 | デジタル戦略部 |
| 1.7 | 2026/2/20 | 引き継ぎ前整合性修正: HNSWパラメータ（m=10, efSearch=1000）を実装に合わせ更新、Step 13.3に検索チューニングパラメータ表を追加、Step 15.1にローカル/Azure環境変数対応表を追加 | デジタル戦略部 |

---

## 1. 概要E

### 1.1 目皁E

本手頁E��は、Phase2 PoC「事務改宁E影響候補検�EシスチE��」�EAzure環墁E��構築するため�E手頁E��記載する。要件定義書�E�EEQ-FAQ-IMPACT-002 v3.2�E�に基づき、Week 1、Eで忁E��なリソースの作�E・設定を行う、E

### 1.2 構築対象リソース

| # | リソース | SKU/プラン | 用送E|
|---|---------|-----------|------|
| 1 | リソースグルーチE|  E| 全リソースの論理グルーチE|
| 2 | Azure OpenAI | Standard S0 | text-embedding-3-large�E�E,072次允E��E|
| 3 | Azure AI Search | Basic | ハイブリチE��検索、Semantic Ranker、Indexer |
| 4 | Cosmos DB | Serverless�E�EoSQL API�E�E| シナリオ・FAQ・影響判定�EスタチE�Eタ |
| 5 | Key Vault | Standard | シークレチE��管琁E|
| 6 | Application Insights |  E| 監視�Eログ |
| 7 | App Service�E�Eeb App�E�E| Basic B1 | BotバックエンチE|
| 8 | Azure Bot Service | F0�E�Eree�E�E| Teamsチャネル登録�E�Eingle-Tenant�E�※M365 Agents Toolkit で自動作�E |

### 1.3 全体構築フロー

```
Step 1: リソースグループ作�E
  ↁE
Step 2: Azure OpenAI 作�E + EmbeddingモチE��チE�Eロイ
  ↁE
Step 3: Azure AI Search 作�E + Semantic Ranker有効匁E
  ↁE
Step 4: Cosmos DB 作�E + コンチE��作�E
  ↁE
Step 5: Key Vault 作�E
  ↁE
Step 6: Application Insights 作�E
  ↁE
Step 7: App Service (Web App) 作�E + Managed Identity有効匁E
  ↁE
Step 8: M365 Agents Toolkit セチE��アチE�E + Provision
        �E�Entra IDアプリ登録・Bot Service・Teamsチャネルを�E動作�E�E�E
  ↁE
Step 9: Managed Identityロール付丁E+ Graph API権限付与！EPO連携用�E�E
  ↁE
Step 10: AI Search インチE��クス・チE�Eタソース・Skillset・Indexer設宁E
  ↁE
Step 11: Botアプリケーション実裁E+ Toolkit Deploy
  ↁE
Step 12: Teamsアプリ登録�E�Eoolkit自動生成�Eニフェスト使用�E�E
  ↁE
Step 13: 動作確認！E5ローカルチE��チE�� + 本番チE��ト！E
```

---

## 2. 前提条件

### 2.1 Azureサブスクリプション

| 頁E�� | 要件 |
|------|------|
| サブスクリプション | 有効なAzureサブスクリプション |
| 権陁E| サブスクリプションの「所有老E��また�E「�E同作�E老E��ロール |
| リージョン | Japan East�E�東日本�E�を推奨。Semantic RankerとAzure OpenAIが利用可能であること |
| Azure OpenAI利用申諁E| 事前にAzure OpenAIの利用申請が承認済みであること |

### 2.2 Microsoft 365 / Teams

| 頁E�� | 要件 |
|------|------|
| Microsoft 365ライセンス | Teams利用可能なライセンス |
| Teams管琁E��E��陁E| カスタムアプリのサイドロードが許可されてぁE��こと |
| Entra ID | アプリ登録の権限（アプリケーション管琁E��E��上！E|

### 2.3 開発環墁E

| チE�Eル | バ�Eジョン | 用送E|
|--------|----------|------|
| Azure CLI | 2.60以丁E| リソース作�E・ロール付丁E|
| Node.js | 20 LTS以丁E| Botアプリケーション開発 |
| Visual Studio Code | 最新牁E| 開発エチE��タ |
| M365 Agents Toolkit拡張機�E | 最新牁E| VS Code拡張�E�旧Teams Toolkit�E�E|

### 2.4 命名規則

本手頁E��では以下�E命名規則を使用する。`<env>`はPoC環墁E��示す識別子（侁E `poc`�E�に置き換えること、E

| リソース | 命名侁E|
|---------|--------|
| リソースグルーチE| `rg-maintenance-<env>` |
| Azure OpenAI | `aoai-maintenance-<env>` |
| Azure AI Search | `srch-maintenance-<env>` |
| Cosmos DB | `cosmos-maintenance-<env>` |
| Key Vault | `kv-maintenance-<env>` |
| Application Insights | `appi-maintenance-<env>` |
| App Service Plan | `asp-maintenance-<env>` |
| Web App | `app-maintenance-bot-<env>` |
| Azure Bot Service | `bot-maintenance-<env>` |

---

## 3. Step 1: リソースグループ作�E

**Azureポ�Eタル手頁E**

1. Azure Portal�E�Ettps://portal.azure.com�E�にサインイン
2. 「リソースグループ」�E「作�E」を選抁E
3. 以下を入劁E

| 頁E�� | 値 |
|------|-----|
| サブスクリプション | 対象サブスクリプション |
| リソースグループ名 | `rg-maintenance-poc` |
| リージョン | Japan East |

4. 「確認およ�E作�E」�E「作�E」を選抁E

**Azure CLI:**

```bash
az group create \
  --name rg-maintenance-poc \
  --location japaneast
```

---

## 4. Step 2: Azure OpenAI リソース作�E

### 4.1 リソース作�E

1. Azure Portal →「リソースの作�E」�E「Azure OpenAI」を検索
2. 以下を入劁E

| 頁E�� | 値 |
|------|-----|
| サブスクリプション | 対象サブスクリプション |
| リソースグルーチE| `rg-maintenance-poc` |
| リージョン | Japan East |
| 名前 | `aoai-maintenance-poc` |
| 価格レベル | Standard S0 |

3. ネットワーク: 「すべてのネットワーク」！EoCのため。本番ではプライベ�Eトエンド�Eイント推奨�E�E
4. 「確認して作�E」�E「作�E、E

**Azure CLI:**

```bash
az cognitiveservices account create \
  --name aoai-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --kind OpenAI \
  --sku S0 \
  --location japaneast
```

### 4.2 EmbeddingモチE��のチE�Eロイ

1. 作�EしたAzure OpenAIリソースに移勁E
2. 「モチE��チE�Eロイ」�E「デプロイの管琁E���E Azure AI Foundry�E�旧Azure OpenAI Studio�E�が開く
3. 「デプロイ」�E「モチE��のチE�Eロイ」�E「基本モチE��をデプロイする」を選抁E
4. 以下を設宁E

| 頁E�� | 値 |
|------|-----|
| モチE�� | text-embedding-3-large |
| チE�Eロイ吁E| `text-embedding-3-large` |
| チE�Eロイの種顁E| Standard |
| TPMレート制陁E| 120K�E�推奨、Endexer実行中にスロチE��リングを避けるため�E�E|

5. 「デプロイ」を選抁E

**注愁E** チE�Eロイ名�EAI SearchのSkillset/Vectorizer設定で使用するため、正確に記録すること、E

### 4.3 確認事頁E

- [ ] リソースが正常に作�EされぁE
- [ ] エンド�EインチERLを記録した�E�侁E `https://aoai-maintenance-poc.openai.azure.com/`�E�E
- [ ] EmbeddingモチE��がデプロイ済み
- [ ] チE�Eロイ名を記録した

---

## 5. Step 3: Azure AI Search 作�E

### 5.1 リソース作�E

1. Azure Portal →「リソースの作�E」�E「Azure AI Search」を検索
2. 以下を入劁E

| 頁E�� | 値 |
|------|-----|
| サブスクリプション | 対象サブスクリプション |
| リソースグルーチE| `rg-maintenance-poc` |
| サービス吁E| `srch-maintenance-poc` |
| 場所 | Japan East |
| 価格レベル | **Basic** |

3. 「確認して作�E」�E「作�E、E

**Azure CLI:**

```bash
az search service create \
  --name srch-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --sku basic \
  --location japaneast
```

**価格レベルにBasic以上を選択する理由:**
Semantic RankerはBasic以上で利用可能、Eree tierでは使用不可、E

### 5.2 APIアクセス制御の設定！EBAC有効化！E

BotアプリがManaged IdentityでAI Searchにアクセスするため、ロールベ�Eスのアクセス制御�E�EBAC�E�を有効化する。デフォルト�EAPIキーのみ許可のため、この設定を行わなぁE��RBAC認証で403エラーになる、E

**Azure Portal:**

1. AI Searchリソース →「設定」�E「キー」を選抁E
2. 「APIアクセス制御」セクションで、E*両方**」を選択！EPIキーとロールベ�Eスの両方を許可�E�E
3. 「�EぁE��を押下して確誁E

**Azure CLI:**

```bash
az search service update \
  --name srch-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --auth-options aadOrApiKey \
  --aad-auth-failure-mode http401WithBearerChallenge
```

**注訁E** `aadOrApiKey` はRBAC認証�E�Ezure AD/Entra ID�E�とAPIキー認証の両方を許可する設定。本番環墁E��APIキーを完�Eに無効化する場合�E `--disable-local-auth true` を使用する�E�E--auth-options` とは排他）、E

### 5.3 Semantic Rankerの有効匁E

1. 作�EしたAI Searchリソースに移勁E
2. 左メニュー →「設定」�E「Semantic ranker」を選抁E
3. **Free**プランを選択！EoCでは朁E,000クエリ以冁E�Eため十�E�E�E
4. 「�Eランの選択」を押丁E

**注訁E** Free プランは朁E,000クエリまで。趁E��した場合�EStandardプラン�E�従量課金）に刁E��替え可能。PoCの検証規模ではFreeプランで十�E、E

### 5.4 Managed Identity�E�シスチE��割り当て�E��E有効匁E

AI SearchのIndexerがCosmos DBに接続する際にManaged Identityを使用するため、有効化する、E

1. AI Searchリソース →「設定」�E「ID」を選抁E
2. 「シスチE��割り当て」タチE→「状態」を**オン**に刁E��替ぁE
3. 「保存」を選抁E

### 5.5 確認事頁E

- [ ] AI Searchリソースが正常に作�Eされた！Easic SKU�E�E
- [ ] APIアクセス制御が「両方」に設定された�E�EBAC + APIキー�E�E
- [ ] Semantic Rankerが有効化された�E�Ereeプラン�E�E
- [ ] シスチE��割り当てManaged Identityが有効
- [ ] サービスURLを記録した�E�侁E `https://srch-maintenance-poc.search.windows.net`�E�E
- [ ] 管琁E��ーを記録した�E�Eey Vault格納用�E�E

---

## 6. Step 4: Cosmos DB 作�E

### 6.1 リソース作�E

1. Azure Portal →「リソースの作�E」�E「Azure Cosmos DB」を検索
2. 「Azure Cosmos DB for NoSQL」を選抁E→「作�E、E
3. 以下を入劁E

| 頁E�� | 値 |
|------|-----|
| サブスクリプション | 対象サブスクリプション |
| リソースグルーチE| `rg-maintenance-poc` |
| アカウント名 | `cosmos-maintenance-poc` |
| 場所 | Japan East |
| 容量モーチE| **サーバ�Eレス** |

4. 「確認して作�E」�E「作�E、E

**Azure CLI:**

```bash
az cosmosdb create \
  --name cosmos-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --locations regionName=japaneast \
  --capabilities EnableServerless
```

### 6.2 チE�Eタベ�EスとコンチE��の作�E

Cosmos DBリソース作�E後、データエクスプローラーで以下を作�Eする、E

**チE�Eタベ�Eス作�E:**

1. 「データ エクスプローラー」�E「New Database、E
2. Database id: `maintenance-db`

**コンチE��作�E�E�Eつ�E�E**

| コンチE��吁E| パ�EチE��ションキー | 用送E|
|-----------|-----------------|------|
| `scenarios` | `/categoryId` | シナリオマスタチE�Eタ |
| `faqs` | `/categoryId` | FAQマスタチE�Eタ |
| `impactAssessments` | `/searchId` | 要修正フラグの判定記録 |

吁E��ンチE��の作�E手頁E

1. `maintenance-db`を右クリチE�� →「New Container、E
2. Container id と Partition key を上表の通り入劁E
3. 「OK」を選抁E

**Azure CLI:**

```bash
# チE�Eタベ�Eス作�E
az cosmosdb sql database create \
  --account-name cosmos-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --name maintenance-db

# scenariosコンチE��
az cosmosdb sql container create \
  --account-name cosmos-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --database-name maintenance-db \
  --name scenarios \
  --partition-key-path /categoryId

# faqsコンチE��
az cosmosdb sql container create \
  --account-name cosmos-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --database-name maintenance-db \
  --name faqs \
  --partition-key-path /categoryId

# impactAssessmentsコンチE��
az cosmosdb sql container create \
  --account-name cosmos-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --database-name maintenance-db \
  --name impactAssessments \
  --partition-key-path /searchId
```

### 6.3 Soft Delete検知用プロパティの確誁E

`scenarios`と`faqs`コンチE��のドキュメントには`isDeleted`フィールドを含める、EI Search Indexerはこ�Eフィールドを参�EしてSoft Delete検知を行う。データ投�E時にチE��ォルト値`false`を設定すること、E

### 6.4 確認事頁E

- [ ] Cosmos DBアカウントが正常に作�Eされた！Eerverless�E�E
- [ ] `maintenance-db`チE�Eタベ�Eスが作�EされぁE
- [ ] 3つのコンチE��が作�Eされた！Escenarios`, `faqs`, `impactAssessments`�E�E
- [ ] エンド�EインチERLを記録した�E�侁E `https://cosmos-maintenance-poc.documents.azure.com:443/`�E�E

---

## 7. Step 5: Key Vault 作�E

1. Azure Portal →「リソースの作�E」�E「Key Vault」を検索
2. 以下を入劁E

| 頁E�� | 値 |
|------|-----|
| サブスクリプション | 対象サブスクリプション |
| リソースグルーチE| `rg-maintenance-poc` |
| Key Vault吁E| `kv-maintenance-poc` |
| リージョン | Japan East |
| 価格レベル | Standard |
| アクセス許可モチE�� | Azure ロールベ�Eスのアクセス制御�E�EBAC�E�E|

3. 「確認して作�E」�E「作�E、E

**シークレチE��登録�E�忁E��に応じて�E�E**

API キーをManaged Identityではなくキーで認証する場合、以下をシークレチE��として登録する、E

| シークレチE��吁E| 値 |
|--------------|-----|
| `AiSearchAdminKey` | AI Searchの管琁E��ー |
| `CosmosDbKey` | Cosmos DBの主キー�E�Eanaged Identity使用時�E不要E��E|

**注訁E** 本手頁E��ではManaged Identityによる認証を基本方針とするため、Key VaultのシークレチE��は最小限とする、E

---

## 8. Step 6: Application Insights 作�E

1. Azure Portal →「リソースの作�E」�E「Application Insights」を検索
2. 以下を入劁E

| 頁E�� | 値 |
|------|-----|
| サブスクリプション | 対象サブスクリプション |
| リソースグルーチE| `rg-maintenance-poc` |
| 名前 | `appi-maintenance-poc` |
| リージョン | Japan East |
| ワークスペ�Eス | 既存�ELog Analyticsワークスペ�Eス、また�EチE��ォルチE|

3. 「確認して作�E」�E「作�E、E
4. 作�E後、「接続文字�E」を記録�E�Eotアプリケーション設定で使用�E�E

---

## 9. Step 7: App Service (Web App) 作�E

### 9.1 App Serviceプランの作�E

1. Azure Portal →「リソースの作�E」�E「App Service」を検索
2. 以下を入劁E

| 頁E�� | 値 |
|------|-----|
| サブスクリプション | 対象サブスクリプション |
| リソースグルーチE| `rg-maintenance-poc` |
| 名前 | `app-maintenance-bot-poc` |
| 公閁E| コーチE|
| ランタイムスタチE�� | Node 20 LTS |
| オペレーチE��ングシスチE�� | Linux |
| 地埁E| Japan East |
| App Serviceプラン | 新規作�E: `asp-maintenance-poc`、SKU: **Basic B1** |

3. 「確認して作�E」�E「作�E、E

### 9.2 シスチE��割り当てManaged Identityの有効匁E

1. 作�EしたWeb Appに移勁E
2. 左メニュー →「設定」�E「ID、E
3. 「シスチE��割り当て」タチEↁE状態を**オン**
4. 「保存」�E 確認ダイアログで「�EぁE��E
5. 表示されめE*オブジェクチED�E��EリンシパルID�E�E* を記録�E�ロール付与で使用�E�E

### 9.3 アプリケーション設宁E

Web App →「設定」�E「環墁E��数」で以下を設宁E

| 名前 | 値 | 備老E|
|------|-----|------|
| `MicrosoftAppId` | �E�Etep 8で取得！E| BotのアプリケーションID |
| `MicrosoftAppPassword` | �E�Etep 8で取得！E| BotのクライアントシークレチE�� |
| `MicrosoftAppTenantId` | �E�テナンチED�E�E| Single-Tenant構�Eで忁E��E|
| `MicrosoftAppType` | `SingleTenant` | Single-Tenant構�Eを�E示 |
| `AI_SEARCH_ENDPOINT` | `https://srch-maintenance-poc.search.windows.net` | |
| `AI_SEARCH_INDEX_NAME` | `maintenance-search-index` | |
| `COSMOS_DB_ENDPOINT` | `https://cosmos-maintenance-poc.documents.azure.com:443/` | |
| `COSMOS_DB_DATABASE` | `maintenance-db` | |
| `APPLICATIONINSIGHTS_CONNECTION_STRING` | �E�Etep 6で取得！E| |

**注訁E**
- `MicrosoftAppId`と`MicrosoftAppPassword`はStep 8のBot Service作�E後に設定すめE
- Azure OpenAI関連の環墁E��数�E�エンド�Eイント、デプロイ名）�E不要、Embeddingの呼び出し�EAI Search経由�E�ビルトインSkill/Vectorizer�E�で行い、BotアプリからAzure OpenAIを直接呼び出すことはなぁE��要件定義書8.4参�E�E�E

### 9.4 確認事頁E

- [ ] Web Appが正常に作�Eされた！Eode 20 LTS, Linux, B1�E�E
- [ ] Managed Identityが有効化され、オブジェクチEDを記録した
- [ ] メチE��ージングエンド�EインチERL: `https://app-maintenance-bot-poc.azurewebsites.net/api/messages`

---

## 10. Step 8: M365 Agents Toolkit セチE��アチE�E + Provision

本スチE��プでは、VS CodeのM365 Agents Toolkit拡張機�Eを使用して、以下を自動化する:

- Entra IDアプリ登録�E�Eingle-Tenant�E�E クライアントシークレチE��生�E
- Azure Bot Service作�E�E�E0�E�E メチE��ージングエンド�Eイント設宁E
- Teamsチャネル有効匁E

**注訁E** Multi-Tenant構�Eは2025年7朁E1日以降、新規作�Eが非推奨となった。本手頁E��はSingle-Tenant + クライアントシークレチE��構�Eを使用する。本番環墁E��は User-Assigned Managed Identity への移行を推奨する、E

### 10.1 VS Code拡張機�Eのインスト�Eル

1. VS Codeの拡張機�Eマ�EケチE��プレイス�E�Etrl+Shift+X�E�を開く
2. 「Microsoft 365 Agents Toolkit」を検索
3. インスト�Eル後、VS Codeを�E読み込み
4. 左サイドバーに**M365 Agents Toolkit アイコン**�E�ドキュメンチE稲妻マ�Eク、下部に「M365」表記）が表示されることを確誁E

**参老E** https://marketplace.visualstudio.com/items?itemName=TeamsDevApp.ms-teams-vscode-extension

### 10.2 Azureアカウント�Eサインイン

<!-- スクリーンショチE��: step8-toolkit-signin.png -->

1. 左サイドバーの **M365 Agents Toolkit アイコン**�E�ドキュメンチE稲妻マ�Eク、下部に「M365」表記）をクリチE��
2. 「ACCOUNTS」セクションで **Azure にサインイン**�E�Eadmin@bdxcorp.onmicrosoft.com`、サブスクリプションの所有老E共同作�E老E��限！E
3. サインイン完亁E��、サブスクリプション名、E*Azure subscription 1**」が表示されることを確誁E

> **注愁E** Microsoft 365 へのサインインはこ�E時点では不要。Provision実行時�E�Etep 10.6�E�に求められるため、そのタイミングで行う、E

### 10.3 プロジェクト作�E

プロジェクトタイチEↁEチE��プレーチEↁE言誁EↁEフォルダ ↁEアプリ名�E頁E��選択する、E

1. M365 Agents Toolkit サイドバーで、E*新しいエージェンチEアプリの作�E**」！Ereate a New Agent/App�E�をクリチE��
2. プロジェクトタイプ選抁E **Custom Engine Agent**�E�EI Agentグループ�E�E�E
3. チE��プレート選抁E **Basic Custom Engine Agent**�E�E365 Agents SDKチE��プレート！E
4. LLMサービス選抁E **Azure OpenAI** を選択し、以下を入力（※後で`m365agents.yml`カスタマイズ時に変更するため仮値で可�E�E
   - Azure OpenAI Key: 任意�E仮値
   - Azure OpenAI Endpoint: `https://aoai-maintenance-poc.openai.azure.com/`
   - Model Name: `text-embedding-3-large`
5. プログラミング言誁E **TypeScript**
6. Default folder: `C:\VSCode\rag\rag-maintenance\`
7. Application Name: `maintenance-bot`

> **補足:** チE��プレート選択時の全体構造は以下�E通り。本手頁E��はBotアプリからAzure OpenAIを直接呼び出さなぁE��針（要件定義書8.4参�E�E�だが、M365 Agents SDKベ�Eスのプロジェクト構造を得るためCustom Engine Agentを選択する、ELM接続部刁E�EStep 10.4で削除・変更する、E
>
> - **AI Agent** ↁEDeclarative Agent�E�Eo plugin / With API Plugin�E�E
> - **AI Agent** ↁE**Custom Engine Agent**�E�E*Basic Custom Engine Agent** / Weather Agent�E��E 本手頁E��選抁E
> - **AI Agent** ↁECopilot Connector�E�Eraph Connector�E�E
> - **Apps for Microsoft 365** ↁETeams Agents and Apps�E�Eeneral Teams Agent / Simple Bot 等！E
> - **Apps for Microsoft 365** ↁEOffice Add-in�E�Excel / Word / Outlook�E�E

**生�Eされる�Eロジェクト構造�E�Eoolkit v6系�E�E**

```
maintenance-bot/
├── .vscode/
━E  ├── extensions.json
━E  ├── launch.json            # チE��チE��プロファイル
━E  ├── settings.json
━E  └── tasks.json             # ビルチEチE��チE��タスク
├── appPackage/
━E  ├── manifest.json          # Teamsアプリマニフェスト！E{{変数名}}プレースホルダー�E�E
━E  ├── color.png              # カラーアイコン�E�E92x192�E�E
━E  └── outline.png            # アウトラインアイコン�E�E2x32�E�E
├── infra/
━E  ├── azure.bicep            # Azureリソース定義
━E  ├── azure.parameters.json  # パラメータファイル
━E  └── botRegistration/
━E      └── azurebot.bicep     # Bot Service定義
├── env/
━E  ├── .env.local             # ローカル環墁E��数
━E  ├── .env.local.user        # ローカル機寁E��報�E�Eitignore済み�E�E
━E  ├── .env.dev               # リモート環墁E��数
━E  ├── .env.dev.user          # リモート機寁E��報�E�Eitignore済み�E�E
━E  ├── .env.playground        # Playground環墁E
━E  ├── .env.playground.user
━E  ├── .env.sandbox           # Sandbox環墁E
━E  └── .env.sandbox.user
├── src/
━E  ├── index.ts               # エントリポイント！Express server�E�E
━E  ├── agent.ts               # エージェントロジチE���E�ELM呼出し！E
━E  └── config.ts              # 設定読み込み
├── m365agents.yml             # メインライフサイクル定義�E�Erovision/deploy/publish�E�E
├── m365agents.local.yml       # ローカルチE��チE��用オーバ�EライチE
├── m365agents.playground.yml  # Playground用
├── m365agents.sandbox.yml     # Sandbox用
├── .webappignore              # チE�Eロイ除外設宁E
├── web.config                 # IIS設定！Ezure App Service用�E�E
├── package.json
└── tsconfig.json
```

> **注愁E** Toolkit v6系�E�E365 Agents Toolkit�E�ではライフサイクル定義ファイルが旧`teamsapp.yml`から**`m365agents.yml`**に改名されてぁE��、E

### 10.3.1 依存パチE��ージのインスト�Eル

プロジェクト作�E直後に依存パチE��ージをインスト�Eルする:

```bash
cd maintenance-bot
npm install
```

正常に完亁E��ると紁E26パッケージがインスト�Eルされる。`found 0 vulnerabilities` と表示されれ�EOK、E

### 10.4 m365agents.yml のカスタマイズ�E�既存リソース参�E�E�E

生�Eされたデフォルト�E`m365agents.yml`を編雁E��、Step 1、Eで作�E済みのリソースを使用するようカスタマイズする、E

> **注愁E** Toolkit v6系では旧`teamsapp.yml`が`m365agents.yml`に改名されてぁE��、E

**m365agents.yml の provision セクション:**

```yaml
provision:
  # 1. Entra IDアプリ登録�E�Eingle-Tenant�E�E クライアントシークレチE��自動生戁E
  - uses: botAadApp/create
    with:
      name: app-maintenance-bot-poc
    writeToEnvironmentFile:
      botId: BOT_ID
      botPassword: SECRET_BOT_PASSWORD

  # 2. Bot Framework登録 + Teamsチャネル有効匁E
  - uses: botFramework/create
    with:
      botId: ${{BOT_ID}}
      name: bot-maintenance-poc
      messagingEndpoint: https://app-maintenance-bot-poc.azurewebsites.net/api/messages
      description: "事務改宁E影響候補検�EBot"
      channels:
        - name: msteams

  # 3. Teamsアプリ登録
  - uses: teamsApp/create
    with:
      name: maintenance-bot${{APP_NAME_SUFFIX}}
    writeToEnvironmentFile:
      teamsAppId: TEAMS_APP_ID

  # NOTE: arm/deploy は削除。Web App・App Service Plan等�EStep 1、Eで作�E済み、E

  # 4. マニフェストバリチE�Eション + パッケージ匁E
  - uses: teamsApp/validateManifest
    with:
      manifestPath: ./appPackage/manifest.json
  - uses: teamsApp/zipAppPackage
    with:
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build
  - uses: teamsApp/validateAppPackage
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
  - uses: teamsApp/update
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
```

**チE��ォルトから�E変更点:**

- `arm/deploy`アクション�E�Eicepによるリソース作�E�E�を**削除**。Web App・App Service Plan等�EStep 1、Eで作�E済みのため
- `teamsApp/extendToM365`アクションめE*削除**、E365 Copilot統合�EPoC篁E��夁E
- `botAadApp/create`めE*追加**、Entra IDアプリ登録 + クライアントシークレチE��自動生成を実行する！EgenerateClientSecret`プロパティは不要、�E動で生�Eされる！E
- `botFramework/create`めE*追加**、Ezure Bot Service登録 + Teamsチャネル有効化を実行すめE
- 生�Eされた`BOT_ID`と`SECRET_BOT_PASSWORD`は`env/.env.dev`と`env/.env.dev.user`に自動書き込みされめE
- LLM接続用の環墁E��数�E�EAZURE_OPENAI_API_KEY`等）が`src/config.ts`で参�EされてぁE��場合、BotからAzure OpenAIを直接呼び出さなぁE��針�Eため不要。削除するか空値にする

### 10.5 環墁E��数ファイルの設宁E

**env/.env.dev:**�E�手動記�Eが忁E��な頁E��に `←手動` と記載！E

```env
# Built-in environment variables
TEAMSFX_ENV=dev
APP_NAME_SUFFIX=dev

# 既存リソース惁E�� ←手動記�E
AZURE_SUBSCRIPTION_ID=5a471c83-2f59-4509-806a-3c1fc77bb89a
AZURE_RESOURCE_GROUP_NAME=rg-maintenance-poc

# Provision実行後に自動書き込みされめE
# BOT_ID=<自動生戁E
# TEAMS_APP_ID=<自動生戁E

# 既存リソースのエンド�EインチE←手動記�E�E�Eot実裁E��に使用�E�E
AI_SEARCH_ENDPOINT=https://srch-maintenance-poc.search.windows.net
AI_SEARCH_INDEX_NAME=maintenance-search-index
COSMOS_DB_ENDPOINT=https://cosmos-maintenance-poc.documents.azure.com:443/
COSMOS_DB_DATABASE=maintenance-db
```

**env/.env.dev.user:**�E�Erovision後に自動書き込み、手動編雁E��要E��E

```env
# Provision実行後に自動書き込みされめE
# SECRET_BOT_PASSWORD=<自動生戁E
```

> **注愁E** `m365agents.yml` の deploy セクションで Web App のリソースIDを直接持E��してぁE��ため、`APP_SERVICE_NAME` 環墁E��数は不要、E

### 10.6 Provision実衁E

> **前提:** VS Codeで `maintenance-bot` フォルダを開ぁE��ぁE��こと�E�Em365agents.yml` がワークスペ�Eスルートにある忁E��がある�E�。親フォルダ�E�Erag-maintenance`�E�で開いてぁE��場合、Provisionコマンドが表示されなぁE��合がある、E

**方法A: サイドバーから実行（推奨�E�E*

1. 左サイドバーの **M365 Agents Toolkit アイコン**�E�ドキュメンチE稲妻マ�Eク、下部に「M365」表記）をクリチE��
2. **LIFECYCLE** セクションの、E*Provision**」をクリチE��
3. 環墁E `dev` を選抁E
4. **Microsoft 365 へのサインイン**が求められめEↁE`admin@bdxcorp.onmicrosoft.com`�E�Eeams管琁E��E��限）でサインイン
5. サブスクリプション: `Azure subscription 1` を選抁E

**方法B: コマンドパレチE��から実衁E*

1. Ctrl+Shift+P →、E*Microsoft 365 Agents: Provision**」を検索・選抁E
2. 以降�E方法Aの手頁E、Eと同じ

> **注愁E** コマンドパレチE��のカチE��リは「Microsoft 365 Agents」（「M365 Agents Toolkit」ではなぁE��。`m365agents.yml`が存在するプロジェクトを開いてぁE��ぁE��コマンドが表示されなぁE��E

**Toolkitが以下を自動実衁E**

- Entra IDアプリ登録�E�Eingle-Tenant�E�E
- クライアントシークレチE��生�E
- Azure Bot Service登録�E�E0�E�E メチE��ージングエンド�Eイント設宁E
- Teamsチャネル有効匁E
- Teamsアプリ登録・パッケージ匁E

**完亁E��E** `env/.env.dev`に`BOT_ID`が、`env/.env.dev.user`に`SECRET_BOT_PASSWORD`が書き込まれる

### 10.7 Web Appアプリケーション設定�E登録

Provision完亁E��、`env/.env.dev`から取得した値をWeb Appの環墁E��数に設定する、E

**値の確誁E**

| 変数 | 取得�E |
| ------ | -------- |
| `BOT_ID` | `env/.env.dev` の `BOT_ID=` 衁E|
| `SECRET_BOT_PASSWORD` | `env/.env.dev.user` の `SECRET_BOT_PASSWORD=` 衁E|
| `TEAMS_APP_TENANT_ID` | `env/.env.dev` の `TEAMS_APP_TENANT_ID=` 行！Erovision時に自動書き込み�E�E|
| `APPLICATIONINSIGHTS_CONNECTION_STRING` | Step 6 で取得済み |

**Azure CLI:**

```bash
az webapp config appsettings set \
  --resource-group rg-maintenance-poc \
  --name app-maintenance-bot-poc \
  --settings \
    MicrosoftAppId=<BOT_ID> \
    MicrosoftAppPassword=<SECRET_BOT_PASSWORD> \
    MicrosoftAppTenantId=<TEAMS_APP_TENANT_ID> \
    MicrosoftAppType=SingleTenant \
    AI_SEARCH_ENDPOINT=https://srch-maintenance-poc.search.windows.net \
    AI_SEARCH_INDEX_NAME=maintenance-search-index \
    COSMOS_DB_ENDPOINT=https://cosmos-maintenance-poc.documents.azure.com:443/ \
    COSMOS_DB_DATABASE=maintenance-db \
    APPLICATIONINSIGHTS_CONNECTION_STRING="<Step 6で取得した接続文字�E>"
```

> **注意！Eit Bash環墁E��E** Cosmos DBエンド�Eイント等�Eパスが�E動変換される場合、コマンド�E頭に `MSYS_NO_PATHCONV=1` を付ける、E

**注訁E**
- `MicrosoftAppId`と`MicrosoftAppPassword`はProvision実行後に`env/`ファイルに記録されめE
- `MicrosoftAppTenantId`はEntra IDのチE��ンチED�E�Eenv/.env.dev`の`AAD_APP_TENANT_ID`に記録される場合あり！E
- Azure OpenAI関連の環墁E��数は不要E��EmbeddingはAI Search経由で実行。要件定義書8.4参�E�E�E

### 10.8 確認事頁E

- [ ] M365 Agents Toolkit拡張機�Eがインスト�Eル済み
- [ ] Azure / M365 アカウントにサインイン済み
- [ ] プロジェクトが作�Eされた！Emaintenance-bot/`�E�E
- [ ] `m365agents.yml`がカスタマイズ済み�E�既存リソース参�E�E�E
- [ ] Provisionが正常に完亁E��ぁE
- [ ] `BOT_ID`�E�EicrosoftAppId�E�が取得できた
- [ ] `SECRET_BOT_PASSWORD`�E�EicrosoftAppPassword�E�が取得できた
- [ ] Azure PortalでBot Serviceリソースが確認できる
- [ ] Bot ServiceのTeamsチャネルが「実行中」になってぁE��
- [ ] Web Appのアプリケーション設定に全環墁E��数を登録した

---

## 11. Step 9: Managed Identity ロール付丁E

吁E��ソースのManaged Identityに対して、忁E��なアクセス権限を付与する、E

### 11.1 ロール付与一覧

**AI SearchのManaged Identity向け:**

| 対象リソース | ロール吁E| 付与�E | 用送E|
|-------------|---------|--------|------|
| Azure OpenAI | `Cognitive Services OpenAI User` | **AI Search**のManaged Identity | Skillset�E�インチE��シング時）�EVectorizer�E�クエリ時）から�EEmbedding呼び出ぁE|
| Cosmos DB | `Cosmos DB Account Reader` | **AI Search**のManaged Identity | IndexerのチE�Eタソース接続メタチE�Eタ読み取り�E�管琁E�Eレーン�E�E|
| Cosmos DB | `Cosmos DB Built-in Data Reader` | **AI Search**のManaged Identity | IndexerによるCosmos DBドキュメント読み取り�E�データプレーン�E�E|

**Web AppのManaged Identity向け:**

| 対象リソース | ロール吁E| 付与�E | 用送E|
|-------------|---------|--------|------|
| Azure AI Search | `Search Index Data Reader` | Web AppのManaged Identity | 検索クエリ実衁E|
| Azure AI Search | `Search Service Contributor` | Web AppのManaged Identity | インチE��クス操作（任意！E|
| Cosmos DB | `Cosmos DB Built-in Data Contributor` | Web AppのManaged Identity | FAQ削除・判定記録の書き込み�E�データプレーン�E�E|
| Key Vault | `Key Vault Secrets User` | Web AppのManaged Identity | シークレチE��読み取り |

**Microsoft Graph�E�アプリケーション権限！E**

| 権限名 | 付与�E | 用送E|
|--------|--------|------|
| `Files.ReadWrite.All` | Web AppのManaged Identity | FR-015 Excel出力ファイルのSharePointアチE�EローチE|

**注訁E** BotアプリケーションからAzure OpenAIを直接呼び出すことはなぁE��要件定義書8.4参�E�E�、Embeddingの呼び出し�EすべてAI Search経由�E�ビルトインSkill/Vectorizer�E�で行うため、`Cognitive Services OpenAI User`はAI SearchのMIに付与する、E

### 11.2 Azure OpenAI ロール付与！Ezureポ�Eタル�E�E

**AI SearchのMIに付与すめE*�E�EotアプリからAzure OpenAIは直接呼び出さなぁE��めE��、E

1. Azure OpenAIリソースに移勁E
2. 「アクセス制御�E�EAM�E�」�E「ロールの割り当ての追加、E
3. ロール: `Cognitive Services OpenAI User`
4. メンバ�E: 「�Eネ�EジドID」�E 対象のAI Search�E�Esrch-maintenance-poc`�E�を選抁E
5. 「確認と割り当て、E

**Azure CLI:**

```bash
# AI SearchのプリンシパルIDを取征E
SEARCH_PRINCIPAL_ID=$(az search service show \
  --name srch-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --query identity.principalId -o tsv)

# Azure OpenAIのリソースIDを取征E
AOAI_RESOURCE_ID=$(az cognitiveservices account show \
  --name aoai-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --query id -o tsv)

# AI SearchのMIにCognitive Services OpenAI Userを付丁E
az role assignment create \
  --assignee-object-id $SEARCH_PRINCIPAL_ID \
  --assignee-principal-type ServicePrincipal \
  --role "Cognitive Services OpenAI User" \
  --scope $AOAI_RESOURCE_ID
```

### 11.3 Azure AI Search ロール付与！Ezureポ�Eタル�E�E

1. AI Searchリソースに移勁E
2. 「アクセス制御�E�EAM�E�」�E「ロールの割り当ての追加、E
3. ロール: `Search Index Data Reader`
4. メンバ�E: Web AppのManaged Identityを選抁E
5. 「確認と割り当て、E

**Azure CLI:**

```bash
# Web AppのプリンシパルIDを取征E
WEB_APP_PRINCIPAL_ID=$(az webapp identity show \
  --name app-maintenance-bot-poc \
  --resource-group rg-maintenance-poc \
  --query principalId -o tsv)

SEARCH_RESOURCE_ID=$(az search service show \
  --name srch-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --query id -o tsv)

az role assignment create \
  --assignee-object-id $WEB_APP_PRINCIPAL_ID \
  --assignee-principal-type ServicePrincipal \
  --role "Search Index Data Reader" \
  --scope $SEARCH_RESOURCE_ID
```

### 11.4 Key Vault ロール付与！Ezureポ�Eタル�E�E

1. Key Vaultリソースに移勁E
2. 「アクセス制御�E�EAM�E�」�E「ロールの割り当ての追加、E
3. ロール: `Key Vault Secrets User`
4. メンバ�E: Web AppのManaged Identityを選抁E
5. 「確認と割り当て、E

**Azure CLI:**

```bash
KV_RESOURCE_ID=$(az keyvault show \
  --name kv-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --query id -o tsv)

az role assignment create \
  --assignee-object-id $WEB_APP_PRINCIPAL_ID \
  --assignee-principal-type ServicePrincipal \
  --role "Key Vault Secrets User" \
  --scope $KV_RESOURCE_ID
```

### 11.5 Cosmos DB ロール付与！Ezure CLI忁E��！E

Cosmos DBには**管琁E�Eレーン**と**チE�Eタプレーン**の2種類�Eロールがある、E

**管琁E�Eレーンロール:**
Azureポ�EタルのIAMから付与可能、EI SearchのIndexerがデータソース接続を確立する際に忁E��、E

**チE�Eタプレーンロール:**
Azureポ�EタルのIAMには表示されなぁE��Ezure CLIまた�EPowerShellで付与する忁E��がある、E

**Built-in Data ContributorのロールID:** `00000000-0000-0000-0000-000000000002`

**Built-in Data ReaderのロールID:** `00000000-0000-0000-0000-000000000001`

#### 11.5.1 AI Search ↁECosmos DB�E�管琁E�Eレーン: Account Reader�E�E

AI SearchのIndexerがCosmos DBアカウント�EメタチE�Eタを読み取るために忁E��、E

1. Cosmos DBリソースに移勁E
2. 「アクセス制御�E�EAM�E�」�E「ロールの割り当ての追加、E
3. ロール: `Cosmos DB Account Reader Role`
4. メンバ�E: AI SearchのManaged Identity�E�Esrch-maintenance-poc`�E�を選抁E
5. 「確認と割り当て、E

**Azure CLI:**

```bash
COSMOS_RESOURCE_ID=$(az cosmosdb show \
  --name cosmos-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --query id -o tsv)

az role assignment create \
  --assignee-object-id $SEARCH_PRINCIPAL_ID \
  --assignee-principal-type ServicePrincipal \
  --role "Cosmos DB Account Reader Role" \
  --scope $COSMOS_RESOURCE_ID
```

#### 11.5.2 AI Search ↁECosmos DB�E�データプレーン: Data Reader�E�E

```bash
az cosmosdb sql role assignment create \
  --account-name cosmos-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --scope "/" \
  --principal-id $SEARCH_PRINCIPAL_ID \
  --role-definition-id 00000000-0000-0000-0000-000000000001
```

#### 11.5.3 Web App ↁECosmos DB�E�データプレーン: Data Contributor�E�E

```bash
az cosmosdb sql role assignment create \
  --account-name cosmos-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --scope "/" \
  --principal-id $WEB_APP_PRINCIPAL_ID \
  --role-definition-id 00000000-0000-0000-0000-000000000002
```

**注意事頁E**
- Cosmos DBのチE�EタプレーンロールはAzureポ�EタルのIAMには表示されなぁE��管琁E�Eレーンのロールとは別系統のため�E�E
- ロール付与が反映されるまで数刁E��かる場合がある
- チE�Eタプレーンロール付与�E確誁E `az cosmosdb sql role assignment list --account-name cosmos-maintenance-poc --resource-group rg-maintenance-poc`
- 管琁E�Eレーンロール付与�E確誁E `az role assignment list --scope $COSMOS_RESOURCE_ID`

### 11.6 Microsoft Graph API 権限付与！ER-015 SPO連携用�E�E

FR-015のExcel出力機�Eでは、生成したExcelファイルをSharePoint Online�E�EPO�E�にアチE�Eロードする。Web AppのManaged IdentityにMicrosoft Graph APIのアプリケーション権限`Files.ReadWrite.All`を付与する、E

> **注愁E** こ�E権限�EAzureポ�EタルのIAM�E�EBACロール�E�ではなく、Microsoft Graph APIのアプリケーション権限として付与する、Ezureポ�EタルのIAM画面からは付与できなぁE��E

#### 11.6.1 SPOサイチED・ドライブIDの取征E

アチE�Eロード�EのSPOサイト�EサイチEDとドライブID�E�ドキュメントライブラリ�E�を取得する、E

```bash
# SPOサイト�EサイチEDを取得！Ehostname}と{path}は環墁E��合わせて変更�E�E
az rest --method GET \
  --url "https://graph.microsoft.com/v1.0/sites/{hostname}:/sites/{site-path}" \
  --query "id" -o tsv

# 侁E bdxcorp.sharepoint.com の msteams_5107f4 サイト�E場吁E
az rest --method GET \
  --url "https://graph.microsoft.com/v1.0/sites/bdxcorp.sharepoint.com:/sites/msteams_5107f4" \
  --query "id" -o tsv
```

取得したサイチEDを使ってドライブ（ドキュメントライブラリ�E�一覧を取征E

```bash
# ドライブ一覧を取得（サイチEDは上記で取得した値に置換！E
az rest --method GET \
  --url "https://graph.microsoft.com/v1.0/sites/{siteId}/drives" \
  --query "value[].{id:id, name:name}" -o table
```

通常は`Documents`�E�Ehared Documents�E�ライブラリのIDを使用する、E

#### 11.6.2 Graph API アプリケーション権限�E付丁E

```bash
# Web AppのMIプリンシパルIDを取征E
WEB_APP_PRINCIPAL_ID=$(az webapp identity show \
  --name app-maintenance-bot-poc \
  --resource-group rg-maintenance-poc \
  --query principalId -o tsv)

# Microsoft GraphサービスプリンシパルのIDを取征E
GRAPH_SP_ID=$(az ad sp list \
  --filter "appId eq '00000003-0000-0000-c000-000000000000'" \
  --query "[0].id" -o tsv)

# Files.ReadWrite.All の App Role IDを取征E
FILES_RW_ROLE_ID=$(az ad sp list \
  --filter "appId eq '00000003-0000-0000-c000-000000000000'" \
  --query "[0].appRoles[?value=='Files.ReadWrite.All'].id | [0]" -o tsv)

# App Role を付丁E
az rest --method POST \
  --url "https://graph.microsoft.com/v1.0/servicePrincipals/$WEB_APP_PRINCIPAL_ID/appRoleAssignments" \
  --headers "Content-Type=application/json" \
  --body "{
    \"principalId\": \"$WEB_APP_PRINCIPAL_ID\",
    \"resourceId\": \"$GRAPH_SP_ID\",
    \"appRoleId\": \"$FILES_RW_ROLE_ID\"
  }"
```

**付与�E確誁E**

```bash
az rest --method GET \
  --url "https://graph.microsoft.com/v1.0/servicePrincipals/$WEB_APP_PRINCIPAL_ID/appRoleAssignments" \
  --query "value[?appRoleId=='$FILES_RW_ROLE_ID'].{principalDisplayName:principalDisplayName, resourceDisplayName:resourceDisplayName}" -o table
```

`app-maintenance-bot-poc` ↁE`Microsoft Graph` の割り当てが表示されれ�EOK、E

#### 11.6.3 SPO環墁E��数の設宁E

取得したサイチED・ドライブIDを`.localConfigs`と Web Appの環墁E��数に追加する:

```ini
SPO_SITE_ID=<取得したサイチED>
SPO_DRIVE_ID=<取得したドライブID>
SPO_UPLOAD_FOLDER=影響候補シナリオ
```

Web Appへの反映:

```bash
az webapp config appsettings set \
  --name app-maintenance-bot-poc \
  --resource-group rg-maintenance-poc \
  --settings \
    SPO_SITE_ID="<取得したサイチED>" \
    SPO_DRIVE_ID="<取得したドライブID>" \
    SPO_UPLOAD_FOLDER="影響候補シナリオ"
```

### 11.7 確認事頁E

- [ ] Azure OpenAI: **AI Search**に`Cognitive Services OpenAI User`を付与しぁE
- [ ] AI Search: Web Appに`Search Index Data Reader`を付与しぁE
- [ ] Key Vault: Web Appに`Key Vault Secrets User`を付与しぁE
- [ ] Cosmos DB�E�管琁E�Eレーン�E�E AI Searchに`Cosmos DB Account Reader Role`を付与しぁE
- [ ] Cosmos DB�E�データプレーン�E�E AI Searchに`Built-in Data Reader`を付与した！ELI�E�E
- [ ] Cosmos DB�E�データプレーン�E�E Web Appに`Built-in Data Contributor`を付与した！ELI�E�E
- [ ] Microsoft Graph: Web Appに`Files.ReadWrite.All`を付与した！Eraph API�E�E
- [ ] SPOサイチED・ドライブIDを取得し、`.localConfigs`に設定しぁE
- [ ] 吁E��ール付与をCLIで確認しぁE

---

## 12. Step 10: AI Search インチE��クス・チE�Eタソース・Skillset・Indexer設宁E

AI Search REST APIを使用してインチE��シングパイプラインを構築する。以下�E頁E��で作�Eする、E

### 12.1 概要E

```
(1) インチE��クス定義 ↁE(2) チE�Eタソース ↁE(3) Skillset ↁE(4) Indexer
```

以下�EREST API呼び出しでは、AI Searchの管琁E��ー�E�また�E Managed Identity + Entra ID認証�E�を使用する、E

**共通�EチE��ー:**

```
Content-Type: application/json
api-key: <AI Searchの管琁E��ー>
```

### 12.2 インチE��クス作�E

```
PUT https://srch-maintenance-poc.search.windows.net/indexes/maintenance-search-index?api-version=2025-09-01
```

```json
{
  "name": "maintenance-search-index",
  "fields": [
    { "name": "id", "type": "Edm.String", "key": true, "filterable": true },
    { "name": "dataType", "type": "Edm.String", "filterable": true },
    { "name": "categoryId", "type": "Edm.String", "filterable": true },
    { "name": "categoryName", "type": "Edm.String", "searchable": true, "filterable": true, "analyzer": "ja.microsoft" },
    { "name": "title", "type": "Edm.String", "searchable": true, "analyzer": "ja.microsoft" },
    { "name": "content", "type": "Edm.String", "searchable": true, "analyzer": "ja.microsoft" },
    { "name": "combinedContent", "type": "Edm.String", "searchable": true, "analyzer": "ja.microsoft" },
    {
      "name": "contentVector",
      "type": "Collection(Edm.Single)",
      "searchable": true,
      "dimensions": 3072,
      "vectorSearchProfile": "vector-profile"
    },
    { "name": "keywords", "type": "Collection(Edm.String)", "searchable": true, "filterable": true },
    { "name": "updatedAt", "type": "Edm.DateTimeOffset", "filterable": true, "sortable": true },
    { "name": "isDeleted", "type": "Edm.Boolean", "filterable": true }
  ],
  "vectorSearch": {
    "algorithms": [
      {
        "name": "hnsw-algorithm",
        "kind": "hnsw",
        "hnswParameters": {
          "m": 10,
          "efConstruction": 400,
          "efSearch": 1000,
          "metric": "cosine"
        }
      }
    ],
    "vectorizers": [
      {
        "name": "openai-vectorizer",
        "kind": "azureOpenAI",
        "azureOpenAIParameters": {
          "resourceUri": "https://aoai-maintenance-poc.openai.azure.com",
          "deploymentId": "text-embedding-3-large",
          "modelName": "text-embedding-3-large"
        }
      }
    ],
    "profiles": [
      {
        "name": "vector-profile",
        "algorithm": "hnsw-algorithm",
        "vectorizer": "openai-vectorizer"
      }
    ]
  },
  "semantic": {
    "configurations": [
      {
        "name": "semantic-config",
        "prioritizedFields": {
          "titleField": { "fieldName": "title" },
          "prioritizedContentFields": [
            { "fieldName": "content" },
            { "fieldName": "combinedContent" }
          ],
          "prioritizedKeywordsFields": [
            { "fieldName": "keywords" }
          ]
        }
      }
    ]
  }
}
```

### 12.3 チE�Eタソース作�E

要件定義書の設計に基づき、`scenarios`と`faqs`は別コンチE��であるため、データソースめEつ作�Eする、E

**チE�Eタソース1: scenarios**

```
POST https://srch-maintenance-poc.search.windows.net/datasources?api-version=2025-09-01
```

```json
{
  "name": "cosmos-scenarios-ds",
  "type": "cosmosdb",
  "credentials": {
    "connectionString": "ResourceId=/subscriptions/<subscription-id>/resourceGroups/rg-maintenance-poc/providers/Microsoft.DocumentDB/databaseAccounts/cosmos-maintenance-poc;Database=maintenance-db;"
  },
  "container": {
    "name": "scenarios",
    "query": null
  },
  "dataChangeDetectionPolicy": {
    "@odata.type": "#Microsoft.Azure.Search.HighWaterMarkChangeDetectionPolicy",
    "highWaterMarkColumnName": "_ts"
  },
  "dataDeletionDetectionPolicy": {
    "@odata.type": "#Microsoft.Azure.Search.SoftDeleteColumnDeletionDetectionPolicy",
    "softDeleteColumnName": "isDeleted",
    "softDeleteMarkerValue": "true"
  }
}
```

**チE�Eタソース2: faqs**

```
POST https://srch-maintenance-poc.search.windows.net/datasources?api-version=2025-09-01
```

```json
{
  "name": "cosmos-faqs-ds",
  "type": "cosmosdb",
  "credentials": {
    "connectionString": "ResourceId=/subscriptions/<subscription-id>/resourceGroups/rg-maintenance-poc/providers/Microsoft.DocumentDB/databaseAccounts/cosmos-maintenance-poc;Database=maintenance-db;"
  },
  "container": {
    "name": "faqs",
    "query": null
  },
  "dataChangeDetectionPolicy": {
    "@odata.type": "#Microsoft.Azure.Search.HighWaterMarkChangeDetectionPolicy",
    "highWaterMarkColumnName": "_ts"
  },
  "dataDeletionDetectionPolicy": {
    "@odata.type": "#Microsoft.Azure.Search.SoftDeleteColumnDeletionDetectionPolicy",
    "softDeleteColumnName": "isDeleted",
    "softDeleteMarkerValue": "true"
  }
}
```

**注訁E**
- `connectionString`に`ResourceId`形式を使用することで、AI SearchのSystem Assigned Managed Identityで認証する�E�Etep 11.5で`Cosmos DB Account Reader Role`+`Built-in Data Reader`を付与済み�E�E
- `apiKey`や`identity`フィールド�E持E���E不要E��Eystem Assigned MI使用時�E自動で適用される！E
- `impactAssessments`コンチE��はインチE��クス対象外�Eため、データソースを作�EしなぁE

### 12.4 Skillset作�E

AzureOpenAIEmbeddingSkillを使用して、Indexer実行時に自動でベクトル化する、E

```
PUT https://srch-maintenance-poc.search.windows.net/skillsets/maintenance-skillset?api-version=2025-09-01
```

```json
{
  "name": "maintenance-skillset",
  "skills": [
    {
      "@odata.type": "#Microsoft.Skills.Text.AzureOpenAIEmbeddingSkill",
      "name": "embedding-skill",
      "context": "/document",
      "resourceUri": "https://aoai-maintenance-poc.openai.azure.com",
      "deploymentId": "text-embedding-3-large",
      "modelName": "text-embedding-3-large",
      "dimensions": 3072,
      "inputs": [
        {
          "name": "text",
          "source": "/document/combinedContent"
        }
      ],
      "outputs": [
        {
          "name": "embedding",
          "targetName": "contentVector"
        }
      ]
    }
  ]
}
```

**注訁E**
- `combinedContent`フィールド！Eitle + content の結合チE��スト）をベクトル化�E入力とする、Eosmos DB側でドキュメント格納時に`combinedContent`を事前に生�Eしておく、E
- 上記インチE��クス定義はWeek 1、EのチE��スト検索用。Week 3で画像検索を追加する際�E、`imageVector`フィールド！ECollection(Edm.Single)`, 1,024次允E Azure Vision multimodal embedding用�E�と対応するvectorSearchプロファイルをインチE��クスに追加する、E

### 12.5 Indexer作�E

2つのチE�Eタソースに対応するIndexerをそれぞれ作�Eする。両方とも同一インチE��クス�E�Emaintenance-search-index`�E�と同一Skillset�E�Emaintenance-skillset`�E�を使用する、E

**Indexer 1: scenarios**

```
PUT https://srch-maintenance-poc.search.windows.net/indexers/maintenance-scenarios-indexer?api-version=2025-09-01
```

```json
{
  "name": "maintenance-scenarios-indexer",
  "dataSourceName": "cosmos-scenarios-ds",
  "targetIndexName": "maintenance-search-index",
  "skillsetName": "maintenance-skillset",
  "schedule": {
    "interval": "PT10M"
  },
  "parameters": {
    "batchSize": 100,
    "maxFailedItems": -1,
    "maxFailedItemsPerBatch": -1
  },
  "fieldMappings": [
    { "sourceFieldName": "id", "targetFieldName": "id" },
    { "sourceFieldName": "dataType", "targetFieldName": "dataType" },
    { "sourceFieldName": "categoryId", "targetFieldName": "categoryId" },
    { "sourceFieldName": "categoryName", "targetFieldName": "categoryName" },
    { "sourceFieldName": "title", "targetFieldName": "title" },
    { "sourceFieldName": "content", "targetFieldName": "content" },
    { "sourceFieldName": "combinedContent", "targetFieldName": "combinedContent" },
    { "sourceFieldName": "keywords", "targetFieldName": "keywords" },
    { "sourceFieldName": "updatedAt", "targetFieldName": "updatedAt" },
    { "sourceFieldName": "isDeleted", "targetFieldName": "isDeleted" }
  ],
  "outputFieldMappings": [
    {
      "sourceFieldName": "/document/contentVector",
      "targetFieldName": "contentVector"
    }
  ]
}
```

**Indexer 2: faqs**

```
PUT https://srch-maintenance-poc.search.windows.net/indexers/maintenance-faqs-indexer?api-version=2025-09-01
```

```json
{
  "name": "maintenance-faqs-indexer",
  "dataSourceName": "cosmos-faqs-ds",
  "targetIndexName": "maintenance-search-index",
  "skillsetName": "maintenance-skillset",
  "schedule": {
    "interval": "PT10M"
  },
  "parameters": {
    "batchSize": 100,
    "maxFailedItems": -1,
    "maxFailedItemsPerBatch": -1
  },
  "fieldMappings": [
    { "sourceFieldName": "id", "targetFieldName": "id" },
    { "sourceFieldName": "dataType", "targetFieldName": "dataType" },
    { "sourceFieldName": "categoryId", "targetFieldName": "categoryId" },
    { "sourceFieldName": "categoryName", "targetFieldName": "categoryName" },
    { "sourceFieldName": "title", "targetFieldName": "title" },
    { "sourceFieldName": "content", "targetFieldName": "content" },
    { "sourceFieldName": "combinedContent", "targetFieldName": "combinedContent" },
    { "sourceFieldName": "keywords", "targetFieldName": "keywords" },
    { "sourceFieldName": "updatedAt", "targetFieldName": "updatedAt" },
    { "sourceFieldName": "isDeleted", "targetFieldName": "isDeleted" }
  ],
  "outputFieldMappings": [
    {
      "sourceFieldName": "/document/contentVector",
      "targetFieldName": "contentVector"
    }
  ]
}
```

**`schedule.interval: "PT10M"`** で10分ごとにIndexerが実行される。初回はCosmos DBの全ドキュメントをインデクシングし、以降は`_ts`（Cosmos DB内部タイムスタンプ）の変更分のみ処理する（High Water Mark方式）。

**差分ベクトル化のコストについて:** Indexerの実行コストはポーリング頻度ではなく、**実際に変更されたドキュメント数**に依存する。変更がない場合の空振り実行では、Cosmos DBへの変更チェッククエリ（数RU）のみが発生し、Azure OpenAI Embeddingの呼び出しは行われない。そのため、10分間隔でも1時間間隔でもコスト差はほぼゼロである。5年間の運用でも空振りによる追加コストは数百円程度と見積もられる。

**注訁E** 2つのIndexerが同一インチE��クスに書き込む構�EはAI Searchで正式にサポ�EトされてぁE��。各Indexerは独立したスケジュールで動作し、同一ドキュメント（同一`id`�E�を更新する場合�E後勝ち�E�East-write-wins�E�となる、E

### 12.6 Indexer手動実行と確誁E

両方のIndexerを手動実行すめE

```
POST https://srch-maintenance-poc.search.windows.net/indexers/maintenance-scenarios-indexer/run?api-version=2025-09-01
POST https://srch-maintenance-poc.search.windows.net/indexers/maintenance-faqs-indexer/run?api-version=2025-09-01
```

実行状態�E確誁E

```
GET https://srch-maintenance-poc.search.windows.net/indexers/maintenance-scenarios-indexer/status?api-version=2025-09-01
GET https://srch-maintenance-poc.search.windows.net/indexers/maintenance-faqs-indexer/status?api-version=2025-09-01
```

`lastResult.status`が`success`であれば正常。`transientFailure`の場合�EAzure OpenAIのTPM趁E��による一時的なエラーの可能性があり、�E実行で解消する、E

### 12.7 確認事頁E

- [ ] インチE��クス `maintenance-search-index` が作�EされぁE
- [ ] チE�EタソースぁEつ作�Eされた！Ecosmos-scenarios-ds`, `cosmos-faqs-ds`�E�E
- [ ] Skillsetが作�EされぁE
- [ ] IndexerぁEつ作�Eされ、�E回実行が成功した
- [ ] 検索エクスプローラーでチE�Eタが検索できる

---

## 13. Step 11: Botアプリケーション実裁E+ Toolkit Deploy

### 13.1 SDK選宁E

| SDK | 状慁E| npmパッケージ | 備老E|
|-----|------|-------------|------|
| M365 Agents SDK | **GA**�E�ES, C#, Python�E�E| `@microsoft/agents-hosting`, `@microsoft/agents-hosting-express` | 要件定義書の基本方針。Teams以外�Eチャネルにも対応可能 |
| Teams SDK�E�旧Teams AI Library v2�E�E| GA�E�ES, C#�E�E| `@microsoft/teams-ai` | Teams特化�EBot開発SDK、E365 Agents SDKと併用可能 |
| Bot Framework SDK | **サポ�Eト終亁E*�E�E025/12/31�E�E| `botbuilder` | 新規開発には使用しなぁE|

要件定義書に基づき、本PoCでは**M365 Agents SDK**�E�EypeScript�E�を基本方針とする。Step 10.3で作�EしたToolkitプロジェクトには、SDK依存関係が既に含まれてぁE��、E

### 13.2 追加パッケージのインスト�Eル

ToolkitチE��プレートに含まれなぁE��チE��ージを追加する:

```bash
cd maintenance-bot
npm install @azure/cosmos @azure/identity @azure/search-documents
```

### 13.3 最小構�Eの動作確認コーチE

Step 10.3で生�Eされた`src/index.ts`を編雁E��、まず最小限のエコーBotで動作確認すめE

```typescript
import { AgentApplication, MemoryStorage } from '@microsoft/agents-hosting'
import { startServer } from '@microsoft/agents-hosting-express'

const app = new AgentApplication({
  storage: new MemoryStorage()
})

app.activity('message', async (context) => {
  // 動作確認用: エコー応筁E
  await context.sendActivity(`受信: ${context.activity.text}`)
})

startServer(app)
```

**注訁E** 上記�E動作確認用の最小構�E。検索ロジチE��、Adaptive Card生�E、Cosmos DB書き込み等�E実裁E��細は別途実裁E��イドで扱ぁE��E

> **主要チューニングパラメータ（`src/config.ts` 参照）**
>
> | パラメータ | 現在値 | 説明 |
> |-----------|--------|------|
> | `VECTOR_WEIGHT` | `4.5` | AI Search RRF ベクトル重み（BM25 は暗黙 1.0）。Semantic Ranker 有効時はやや低め推奨 |
> | HNSW `m` | `10` | グラフ接続数（上限10）。高いほど精度向上・インデックスサイズ増大 |
> | HNSW `efSearch` | `1000` | 探索候補数。高いほど精度向上・レイテンシ増大 |
> | `CACHE_TTL_MS` | `30分` | 検索結果インメモリキャッシュの有効期間 |
>
> ※ HNSW `m` を変更した場合はインデックスの**再作成**（DELETE → PUT → Indexer リセット + 実行）が必要。

> **重要: Azure SDKクライアントの遅延初期化**
>
> `SearchClient`�E�E@azure/search-documents`�E�や`CosmosClient`�E�E@azure/cosmos`�E�をモジュールのトップレベルで初期化しなぁE��と。`env-cmd`による環墁E��数の読み込みは、モジュール評価よりも後に行われるケースがあり、エンド�Eイントが空斁E���Eまま`Invalid URL`エラーとなる、E
>
> ```typescript
> // NG: モジュール読み込み時に即座に初期化される
> const client = new SearchClient(config.endpoint, config.index, credential);
>
> // OK: 初回呼び出し時に初期化（遅延初期化パターン�E�E
> let _client: SearchClient | null = null;
> function getSearchClient(): SearchClient {
>   if (!_client) {
>     _client = new SearchClient(config.endpoint, config.index, credential);
>   }
>   return _client;
> }
> ```
>
> `CosmosClient`も同様に遁E��初期化すること、E

### 13.4 m365agents.yml の deploy セクション設宁E

`m365agents.yml`のdeployセクションで既存�EWeb AppへチE�Eロイするよう設定すめE

```yaml
deploy:
  # 1. npm install
  - uses: cli/runNpmCommand
    with:
      args: install

  # 2. TypeScript ビルチE
  - uses: cli/runNpmCommand
    with:
      args: run build

  # 3. 既存Web Appへの ZIPチE�Eロイ
  - uses: azureAppService/zipDeploy
    with:
      artifactFolder: .
      ignoreFile: .deployignore
      resourceId: /subscriptions/${{AZURE_SUBSCRIPTION_ID}}/resourceGroups/${{AZURE_RESOURCE_GROUP_NAME}}/providers/Microsoft.Web/sites/${{APP_SERVICE_NAME}}
```

**`.deployignore`ファイルの作�E:**

```
.git
.vscode
env/
appPackage/
infra/
node_modules/
*.ts
!dist/
m365agents.yml
m365agents.local.yml
m365agents.playground.yml
m365agents.sandbox.yml
.deployignore
```

### 13.5 Toolkit Deployの実衁E

1. VS Code コマンドパレチE���E�Etrl+Shift+P�E��E「M365 Agents Toolkit: Deploy、E
2. 環墁E `dev` を選抁E
3. Toolkitが以下を自動実衁E
   - `npm install`
   - TypeScriptビルチE
   - 既存Web App�E�Eapp-maintenance-bot-poc`�E�へZIPチE�Eロイ
4. チE�Eロイ完亁E�EメチE��ージを確誁E

**Azure CLI でのチE�Eロイ�E�代替方法！E**

```bash
cd maintenance-bot
npm install && npm run build
zip -r deploy.zip . -x "node_modules/*" ".git/*" "env/*" "*.ts"
az webapp deploy \
  --resource-group rg-maintenance-poc \
  --name app-maintenance-bot-poc \
  --src-path deploy.zip \
  --type zip
```

### 13.6 確認事頁E

- [ ] 追加パッケージがインスト�EルされぁE
- [ ] 最小構�EのBotコードが用意された
- [ ] `m365agents.yml`のdeployセクションが設定済み
- [ ] Toolkit Deployが正常に完亁E��ぁE
- [ ] Web Appのログストリームでエラーが�EてぁE��ぁE
- [ ] `https://app-maintenance-bot-poc.azurewebsites.net/api/messages`エンド�Eイントが応答してぁE��

**注訁E** 本スチE��プ�E環墁E��築（デプロイ先�E準備と初回チE�Eロイ�E�を対象とする。検索ロジチE��、Adaptive Card生�E、Cosmos DB書き込み、エラーハンドリング等�E実裁E��細は別途作�Eする実裁E��イドで扱ぁE��E

---

## 14. Step 12: Teamsアプリ登録・サイドローチE

### 14.1 アプリマニフェスト�E編雁E

Step 10.3で生�Eされた`appPackage/manifest.json`を編雁E��る。Toolkitはプレースホルダー構文�E�E${{変数名}}`�E�を使用しており、Provision/Deploy時に自動で実際の値に置換される、E

```json
{
  "$schema": "https://developer.microsoft.com/en-us/json-schemas/teams/v1.17/MicrosoftTeams.schema.json",
  "manifestVersion": "1.17",
  "version": "1.0.0",
  "id": "${{TEAMS_APP_ID}}",
  "developer": {
    "name": "チE��タル戦略部",
    "websiteUrl": "https://chibabank.co.jp",
    "privacyUrl": "https://chibabank.co.jp/privacy",
    "termsOfUseUrl": "https://chibabank.co.jp/terms"
  },
  "name": {
    "short": "影響候補検�EBot",
    "full": "事務改宁E影響候補検�EシスチE�� (Phase2 PoC)"
  },
  "description": {
    "short": "事務改定時の影響候補をAI検索で検�EしまぁE,
    "full": "改定�E容をテキスト�E力すると、影響を受ける可能性のあるシナリオとFAQを検�Eし、一覧表示します、E
  },
  "icons": {
    "outline": "outline.png",
    "color": "color.png"
  },
  "accentColor": "#2B5292",
  "bots": [
    {
      "botId": "${{BOT_ID}}",
      "scopes": ["personal"],
      "supportsFiles": false,
      "isNotificationOnly": false,
      "commandLists": [
        {
          "scopes": ["personal"],
          "commands": [
            {
              "title": "help",
              "description": "使ぁE��を表示しまぁE
            }
          ]
        }
      ]
    }
  ],
  "permissions": ["identity", "messageTeamMembers"],
  "validDomains": [
    "app-maintenance-bot-poc.azurewebsites.net"
  ]
}
```

**注訁E**
- `${{TEAMS_APP_ID}}`と`${{BOT_ID}}`はProvision時に自動生成された値で置換される
- アイコンファイル�E�Ecolor.png`, `outline.png`�E��EToolkitが生成するデフォルトアイコンを使用するか、カスタムアイコンに差し替える

### 14.2 アプリパッケージの自動生戁E

Provision時に`m365agents.yml`の`teamsApp/zipAppPackage`アクションにより、以下が自動実行される:

1. マニフェスト�EバリチE�Eション
2. プレースホルダーの値置揁E
3. ZIPパッケージの生�E�E�EappPackage/build/appPackage.dev.zip`�E�E
4. Teams Developer Portalへの登録

手動でパッケージを作�Eする忁E���EなぁE��E

### 14.3 TeamsへのサイドローチE

**方法A: Toolkit経由�E�推奨�E�E*

1. VS Code コマンドパレチE�� ↁE「M365 Agents Toolkit: Publish」を選抁E
2. 絁E���ETeamsアプリカタログに公開される
3. また�E、F5チE��チE��実行時にToolkitが�E動でサイドロードすめE

**方法B: 手動サイドローチE*

1. `appPackage/build/appPackage.dev.zip`を取征E
2. Microsoft Teamsを開ぁE
3. 左サイドバーの「アプリ」�E「アプリを管琁E���E「アプリのアチE�Eロード、E
4. 「カスタムアプリをアチE�Eロード」を選抁E
5. ZIPファイルを選抁E→「追加、E

**注訁E** サイドロードが無効な場合、Teams管琁E��E��依頼してTeams管琁E��ンター�E�Ettps://admin.teams.microsoft.com�E��E「Teamsアプリ」�E「セチE��アチE�Eポリシー」でカスタムアプリのアチE�Eロードを許可する忁E��がある、E

### 14.4 確認事頁E

- [ ] `appPackage/manifest.json`がカスタマイズ済み
- [ ] アプリパッケージが�E動生成された�E�EappPackage/build/`配下！E
- [ ] Teamsにアプリがインスト�EルされぁE
- [ ] 1:1チャチE��でBotに話しかけられる

---

## 15. Step 13: 動作確認チェチE��リスチE

### 15.0 前提準備�E�テストデータ投�E・権限付与�EIndexer実行！E

動作確認には、Cosmos DBにチE��トデータを投入し、AI SearchインチE��クスに同期する忁E��がある、E

#### 15.0.1 開発老E��けロール付与！EI Search + Cosmos DB�E�E

F5ローカルチE��チE��では`DefaultAzureCredential`が開発老E�EAzure CLIログインアカウントを使用する。Step 9で付与したManaged Identityのロールとは別に、E��発老E��人のアカウントにもロール付与が忁E��、E

**AI Search チE�Eタプレーン�E�検索実行に忁E��E��E**

```bash
# 開発老E�EオブジェクチEDを取征E
DEVELOPER_OID=$(az ad signed-in-user show --query id -o tsv)

# Search Index Data Reader ロールを付丁E
MSYS_NO_PATHCONV=1 az role assignment create \
  --assignee "$DEVELOPER_OID" \
  --role "Search Index Data Reader" \
  --scope "/subscriptions/<サブスクリプションID>/resourceGroups/rg-maintenance-poc/providers/Microsoft.Search/searchServices/srch-maintenance-poc"
```

**注訁E** ロール反映に数刁E��かる場合がある。付与直後に403エラーが�Eる場合�E数刁E��E��てリトライする、E

**Cosmos DB チE�Eタプレーン�E�テストデータ投�Eに忁E��E��E**

ローカルからスクリプトでCosmos DBに書き込むには、E��発老E�EAzure ADアカウントにチE�Eタプレーンのロール付与が忁E��、E

```bash
# Cosmos DB Built-in Data Contributor ロールを付与！EDEVELOPER_OIDは15.0.1で取得済み�E�E
MSYS_NO_PATHCONV=1 az cosmosdb sql role assignment create \
  --account-name cosmos-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --role-definition-id "00000000-0000-0000-0000-000000000002" \
  --principal-id "$DEVELOPER_OID" \
  --scope "/"
```

**注訁E** こ�EロールはチE�Eタプレーン専用であり、Azure Portalの「アクセス制御(IAM)」タブには表示されなぁE��`az cosmosdb sql role assignment list`コマンドで確認する。Web AppのManaged Identity�E�Etep 9で付与済み�E�とは別に、E��発老E��人のアカウントにも付与が忁E��、E

#### 15.0.2 チE��トデータ準備�E�Excel→JSON変換�E�E

rag-local プロジェクト�E実データ�E�Excel�E�を Cosmos DB 投�E用 JSON に変換する、E

**前提:** Python 3.x + openpyxl がインスト�Eル済みであること、E

```bash
# openpyxl をインスト�Eル�E��E回�Eみ�E�E
pip install openpyxl

# Excel→JSON 変換を実衁E
python scripts/convert-excel-to-json.py
```

**入力ファイル�E�Eag-gemini プロジェクト�E�E�E**

| ファイル | 種別 | categoryId |
|---------|------|------------|
| rev03smile_シナリオチE�Eタ_20260203.xlsx | シナリオ | smile |
| rev03souzoku_シナリオチE�Eタ_20260203.xlsx | シナリオ | souzoku |
| rev03naibujimu_シナリオチE�Eタ_20260203.xlsx | シナリオ | naibujimu |
| rev03torikaku_シナリオチE�Eタ_20260203.xlsx | シナリオ | torikaku |
| スマイル_履歴チE�Eタ_20250205.xlsx | FAQ | smile |
| 総則_履歴チE�Eタ_20250829.xlsx | FAQ | sousoku |
| 預��_履歴チE�Eタ_20250830.xlsx | FAQ | yokin |

**出力ファイル:**

| ファイル | 件数 | サイズ |
|---------|------|--------|
| `scripts/data/scenarios.json` | 紁E,300件 | 紁EMB |
| `scripts/data/faqs.json` | 紁E8,700件 | 紁E4MB |

**注訁E** 変換スクリプトはバリチE�Eション付き。空行やチE��トデータ行�E自動スキチE�Eされる。�E力ファイルのパスは `scripts/convert-excel-to-json.py` 冁E�E `RAG_GEMINI_BASE` で設定されてぁE��。異なる環墁E��実行する場合�Eパスを修正すること、E

#### 15.0.3 Cosmos DB チE��トデータ投�E

変換で生�EされぁEJSON めECosmos DB に投�Eする、E

```bash
# maintenance-bot チE��レクトリに移動してから実衁E
cd maintenance-bot

# 初回投�E�E�既存ダミ�EチE�Eタを�E削除してから投�E�E�E
node ../scripts/seed-cosmos.js --clean

# 再投入�E�Epsert で差刁E��新�E�E
node ../scripts/seed-cosmos.js
```

投�Eされるデータ:

| コンチE�� | 件数 | 冁E�� |
|---------|------|------|
| `scenarios` | 紁E,300件 | スマイル(555件)、相綁E269件)、�E部事務(1,384件)、取引時確誁E105件) |
| `faqs` | 紁E8,700件 | スマイル(8,679件)、総則(4,000件)、E����(6,055件) |

**注訁E** スクリプトは `upsert` を使用するため、褁E��回実行しても安�E�E�既存データを上書き）。`--clean` オプションは既存データを�E削除してからクリーンな状態で投�Eする。�E回投入時やスキーマ変更時に使用する。投入は100件ずつバッチ�E琁E��れ、E��捗が表示される、E

#### 15.0.4 AI Search インチE��クス・Indexer 更新

チE��トデータにフィールドが追加されたため、AI Search のインチE��クス定義と Indexer の fieldMappings を更新する、E

```bash
# AI Search管琁E��ーを取征E
ADMIN_KEY=$(az search admin-key show \
  --service-name srch-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --query primaryKey -o tsv)

# インチE��クス定義を更新�E�フィールド追加は非破壊操作！E
MSYS_NO_PATHCONV=1 curl -X PUT \
  "https://srch-maintenance-poc.search.windows.net/indexes/maintenance-search-index?api-version=2024-07-01" \
  -H "Content-Type: application/json" \
  -H "api-key: $ADMIN_KEY" \
  -d @scripts/index-definition.json

# scenarios Indexer を更新
MSYS_NO_PATHCONV=1 curl -X PUT \
  "https://srch-maintenance-poc.search.windows.net/indexers/maintenance-scenarios-indexer?api-version=2024-07-01" \
  -H "Content-Type: application/json" \
  -H "api-key: $ADMIN_KEY" \
  -d @scripts/indexer-scenarios.json

# faqs Indexer を更新
MSYS_NO_PATHCONV=1 curl -X PUT \
  "https://srch-maintenance-poc.search.windows.net/indexers/maintenance-faqs-indexer?api-version=2024-07-01" \
  -H "Content-Type: application/json" \
  -H "api-key: $ADMIN_KEY" \
  -d @scripts/indexer-faqs.json
```

**追加フィールド！Endex-definition.json�E�E**

| フィールド名 | 垁E| 用送E|
|------------|-----|------|
| `path` | Edm.String | シナリオの階層パス |
| `order` | Edm.Int32 | Excel行番号 |
| `isFinalAnswer` | Edm.Boolean | 末端ノ�Eド判宁E|
| `tags` | Edm.String | FAQのタグ斁E���E |

**注訁E** インチE��クスへのフィールド追加は非破壊操作（既存データに影響なし）。ただし、新フィールドに値を�Eれるには Indexer のリセチE���E�E�E実行が忁E��、E

#### 15.0.5 Indexer リセチE���E�E��動実衁E

チE�Eタ投�E後、Indexer をリセチE��して全件再インチE��クス�E��Eベクトル化含む�E�を実行する。`--clean` で既存データを差し替えた場合や、combinedContent のフォーマットが変更された場合�EリセチE��が忁E��、E

```bash
# AI Search管琁E��ーを取得！E5.0.4で取得済みの場合�EスキチE�E可�E�E
ADMIN_KEY=$(az search admin-key show \
  --service-name srch-maintenance-poc \
  --resource-group rg-maintenance-poc \
  --query primaryKey -o tsv)

# scenarios Indexer をリセチE���E��E件再�E琁E��象にする�E�E
curl -X POST \
  "https://srch-maintenance-poc.search.windows.net/indexers/maintenance-scenarios-indexer/reset?api-version=2024-07-01" \
  -H "api-key: $ADMIN_KEY" \
  -H "Content-Length: 0"

# scenarios Indexer を実衁E
curl -X POST \
  "https://srch-maintenance-poc.search.windows.net/indexers/maintenance-scenarios-indexer/run?api-version=2024-07-01" \
  -H "api-key: $ADMIN_KEY" \
  -H "Content-Length: 0"

# faqs Indexer をリセチE��
curl -X POST \
  "https://srch-maintenance-poc.search.windows.net/indexers/maintenance-faqs-indexer/reset?api-version=2024-07-01" \
  -H "api-key: $ADMIN_KEY" \
  -H "Content-Length: 0"

# faqs Indexer を実衁E
curl -X POST \
  "https://srch-maintenance-poc.search.windows.net/indexers/maintenance-faqs-indexer/run?api-version=2024-07-01" \
  -H "api-key: $ADMIN_KEY" \
  -H "Content-Length: 0"
```

HTTP 202が返れば実行開始。紁E1,000件のインチE��クス作�Eにはベクトル化を含むため、完亁E��で数刁E��十数刁E��かる場合がある、E

**確認方況E**

```bash
# scenarios Indexer状態を確誁E
curl -s "https://srch-maintenance-poc.search.windows.net/indexers/maintenance-scenarios-indexer/status?api-version=2024-07-01" \
  -H "api-key: $ADMIN_KEY" | python -c "
import sys,json
d=json.load(sys.stdin)
r=d.get('lastResult',{})
print(f'Status: {r.get(\"status\")}, Items: {r.get(\"itemsProcessed\")}')"

# faqs Indexer状態を確誁E
curl -s "https://srch-maintenance-poc.search.windows.net/indexers/maintenance-faqs-indexer/status?api-version=2024-07-01" \
  -H "api-key: $ADMIN_KEY" | python -c "
import sys,json
d=json.load(sys.stdin)
r=d.get('lastResult',{})
print(f'Status: {r.get(\"status\")}, Items: {r.get(\"itemsProcessed\")}')"
```

**期征E��果:** 両 Indexer で `Status: success` が返り、`Items` ぁEscenarios 紁E,300件 / faqs 紁E8,700件となること、E

### 15.1 F5ローカルチE��チE���E�開発時推奨�E�E

M365 Agents Toolkitの F5 チE��チE��機�Eを使用すると、Botをローカル実行しながらTeamsで動作確認できる、E

**手頁E**

1. VS Codeで`maintenance-bot`プロジェクトを開く
2. **F5キー**を押す（また�E「実行とチE��チE��」�E「Debug in Teams」！E
3. Toolkitが以下を自動実衁E
   - Dev Tunnel�E�Egrok代替�E��E起勁EↁEローカルサーバ�Eをインターネットに公閁E
   - `m365agents.local.yml`に基づくローカルProvision
   - Botアプリのローカル起勁E
   - ブラウザでTeams Web版を自動オープン ↁEBotアプリを�E動サイドローチE
4. Teamsの1:1チャチE��でBotに話しかけて動作確誁E
5. VS CodeのチE��チE��ーでブレークポイント設定�E変数検査が可能

**ローカルチE��チE��の利点:**
- コード変更 ↁE即時反映�E��EチE��リロード！E
- ブレークポイントでスチE��プ実衁E
- console.logがVS CodeのチE��チE��コンソールに表示
- AzureへのチE�Eロイ不要で高速な開発サイクル

**注意事頁E**
- Dev Tunnelの利用にはMicrosoftアカウントでの認証が忁E��E
- 初回実行時にDev Tunnel拡張機�Eのインスト�Eルを求められる場合がある
- ローカルチE��チE��中は`m365agents.local.yml`のProvisionが使用される！Em365agents.yml`とは別�E�E
- `m365agents.local.yml`のdeployセクションに`AZURE_OPENAI_*`系の環墁E��数が含まれてぁE��場合�E**削除すること**。本PoCではBotからAzure OpenAIを直接呼び出さなぁE��め不要であり、Toolkitが未定義変数の入力ダイアログを表示してdeployが失敗する原因となめE

**`.localConfigs`の確認！E5チE��チE��が起動しなぁE��合！E**

F5チE��チE��は`maintenance-bot/.localConfigs`から環墁E��数を読み込む。Toolkit Provisionでこ�Eファイルは自動生成されるが、`clientId`や`clientSecret`が空になってぁE��場合�E手動で設定する、E

```ini
# .localConfigs の忁E��頁E��
clientId=<Bot Service アプリID�E�EOT_ID�E�E
clientSecret=<Entra IDアプリのクライアントシークレチE��>
tenantId=<チE��ンチED>
AI_SEARCH_ENDPOINT=https://srch-maintenance-poc.search.windows.net
AI_SEARCH_INDEX_NAME=maintenance-search-index
COSMOS_DB_ENDPOINT=https://cosmos-maintenance-poc.documents.azure.com:443/
COSMOS_DB_DATABASE=maintenance-db
SPO_SITE_ID=<SPOサイチED�E�Etep 9 11.6.1で取得！E
SPO_DRIVE_ID=<SPOドライブID�E�Etep 9 11.6.1で取得！E
SPO_UPLOAD_FOLDER=影響候補シナリオ
```

`clientId`はStep 8のProvision時に生�EされたBOT_ID�E�Eenv/.env.dev`に記録�E�。`clientSecret`が不�Eな場合�E以下で再生成できる:

```bash
az ad app credential reset \
  --id "<BOT_ID>" \
  --display-name "local-debug" \
  --years 1 \
  --query password -o tsv
```

**注意:** シークレットを再生成すると、既存のシークレットは無効化される。Azure Web Appの環境変数`MicrosoftAppPassword`も同じ値に更新すること。

**ローカル（.localConfigs）と Azure（Web App 環境変数）の対応表:**

| 機能 | ローカル（.localConfigs） | Azure（Web App 環境変数） | 備考 |
|------|--------------------------|--------------------------|------|
| Bot 認証 | `clientId` + `clientSecret` + `tenantId` | `MicrosoftAppId` + `MicrosoftAppPassword` + `MicrosoftAppTenantId` | キー名が異なる |
| Bot タイプ | （自動設定） | `MicrosoftAppType=SingleTenant` | クラウドのみ要設定 |
| AI Search | `AI_SEARCH_ENDPOINT` / `AI_SEARCH_INDEX_NAME` | 同じキー名 | 値も同一 |
| Cosmos DB | `COSMOS_DB_ENDPOINT` / `COSMOS_DB_DATABASE` | 同じキー名 | 値も同一 |
| SharePoint | `SPO_SITE_ID` / `SPO_DRIVE_ID` / `SPO_UPLOAD_FOLDER` | 同じキー名 | 値も同一 |
| App Insights | （不要） | `APPLICATIONINSIGHTS_CONNECTION_STRING` | クラウドのみ |
| 認証方式 | `DefaultAzureCredential` → Azure CLI ログイン | `DefaultAzureCredential` → Managed Identity | コードは同一 |

### 15.2 基本通信確誁E

| # | 確認頁E�� | 手頁E| 期征E��果 |
|---|---------|------|---------|
| 1 | Bot応筁E| Teamsで1:1チャチE��を開き「hello」と入劁E| Botが応答メチE��ージを返す |
| 2 | エラーログ確誁E| Application Insightsまた�EWeb Appのログストリームを確誁E| エラーが�EてぁE��ぁE|

### 15.3 検索機�E確誁E

| # | 確認頁E�� | 手頁E| 期征E��果 |
|---|---------|------|---------|
| 3 | チE��スト検索 | 改定�E容を�E力（侁E「口座開設の本人確認書類が変更」！E| Adaptive Cardで候補一覧が表示されめE|
| 4 | タブ�Eり替ぁE| シナリオタチEFAQタブ�Eボタンを押ぁE| ToggleVisibilityで表示が�Eり替わる |
| 5 | スクロール表示 | 候補数が多い検索結果を確誁E| スクロールバ�Eが表示され、�E件を確認できる |

### 15.4 書き込み機�E確誁E

| # | 確認頁E�� | 手頁E| 期征E��果 |
|---|---------|------|---------|
| 6 | FAQ削除 | FAQタブでチェチE�� →「選択したFAQを削除」�E 確誁E→「削除実行、E| 完亁E��ードが表示され、Cosmos DBの`isDeleted`が`true`に更新 |
| 7 | 要修正フラグ | シナリオタブでチェチE�� →「要修正を保存、E| 完亁E��ードが表示され、Cosmos DBに`impactAssessments`レコードが作�E |
| 8 | Indexer反映 | FAQ削除後、Indexerを手動実行し再検索 | 削除したFAQが検索結果から除夁E|

### 15.5 トラブルシューチE��ング

| 痁E�� | 原因の可能性 | 対処況E|
|------|------------|--------|
| `Invalid URL`でサーバ�Eが起動しなぁE| Azure SDKクライアントがモジュールトップレベルで初期化されてぁE�� | `SearchClient`/`CosmosClient`を遅延初期化パターンに変更�E�Etep 11の注記参照�E�E|
| `Invalid URL`でサーバ�Eが起動しなぁE| `.localConfigs`の`AI_SEARCH_ENDPOINT`等が空 | `.localConfigs`の忁E��頁E��を確認�E設定！E5.1参�E�E�E|
| F5チE��チE��で認証エラー | `.localConfigs`の`clientId`/`clientSecret`が空 | `env/.env.dev`からBOT_IDを転記、シークレチE��は`az ad app credential reset`で再生成！E5.1参�E�E�E|
| Botが応答しなぁE| メチE��ージングエンド�Eイント�E設定誤めE| Bot Service「構�E」�EURLを確誁E|
| 403 Forbidden�E�E5ローカル�E�E| AI SearchのAPIアクセス制御が「APIキーのみ」になってぁE�� | AI Searchリソース→「設定」�E「キー」�E「APIアクセス制御」を「両方」に変更�E�Etep 3の5.2参�E�E�E|
| 403 Forbidden�E�E5ローカル�E�E| 開発老E��カウントにAI Searchのロールが未付丁E| 15.0.1の「AI Search チE�Eタプレーン」ロール付与を実施�E�反映に数刁E��かる�E�E|
| 403 Forbidden�E�Ezure Web App�E�E| Web AppのManaged Identityのロール付与不足 | Step 9のロール付与を再確誁E|
| Indexer失敁E| Azure OpenAI TPM趁E�� | EmbeddingモチE��のレート制限を引き上げ |
| Indexer失敗！Eosmos DB�E�E| チE�EタソースのManaged Identity設定不備 | AI SearchのMI→Cosmos DB Data Readerを確誁E|
| 検索結果ぁE件 | Indexerが未実衁EチE�Eタ未投�E | Indexerを手動実行、Cosmos DBのチE�Eタを確誁E|
| Adaptive Cardが表示されなぁE| カードサイズ28KB趁E�� | 候補件数を確認し、�Eージネ�Eションフォールバックを検訁E|
| Graph APIアチE�Eロード失敗！E03�E�E| Web AppのMIに`Files.ReadWrite.All`が未付丁E| Step 9 11.6.2のGraph API権限付与を実施 |
| Graph APIアチE�Eロード失敗！E04�E�E| `SPO_SITE_ID`また�E`SPO_DRIVE_ID`が誤った値 | Step 9 11.6.1でサイチED・ドライブIDを�E取征E|
| `convert-excel-to-json.py` でファイルが見つからなぁE| Excelファイルのパスが異なめE| スクリプト冁E�E `RAG_GEMINI_BASE` を実際のパスに修正 |
| `seed-cosmos.js` で JSON が見つからなぁE| Excel→JSON 変換が未実衁E| `python scripts/convert-excel-to-json.py` を�Eに実衁E|
| Cosmos DB 投�EがスロチE��リングされめE| Serverless RU 消費が多い | バッチサイズ�E�EATCH_SIZE�E�を50に下げて再実衁E|
| Indexer で一部ドキュメントが失敁E| Embedding API のレート制陁E| Indexer のリトライに任せる、また�E indexer JSON の `batchSize` めE0に下げて再デプロイ |

---

## 付録A: リソースID・エンド�Eイント記録シーチE

環墁E��築完亁E��、以下�E惁E��を記録して管琁E��ること、E

| 頁E�� | 値 |
|------|-----|
| リソースグループ名 | |
| Azure OpenAI エンド�EインチE| |
| Azure OpenAI チE�Eロイ吁E| |
| AI Search エンド�EインチE| |
| AI Search 管琁E��ー | |
| AI Search インチE��クス吁E| |
| Cosmos DB エンド�EインチE| |
| Cosmos DB チE�Eタベ�Eス吁E| |
| Key Vault吁E| |
| Application Insights 接続文字�E | |
| Web App URL | |
| Web App Managed Identity オブジェクチED | |
| Bot Service アプリID�E�EicrosoftAppId�E�E| |
| Bot Service チE��ンチED | |
| Bot Service クライアントシークレチE�� | |

---

## 付録B: 月額コスト概箁E

| リソース | 月額概箁E| 備老E|
|---------|---------|------|
| Azure AI Search (Basic) | ~¥10,000 | Semantic Ranker含む |
| Azure OpenAI (S0) | ~¥2,000 | Embedding利用のみ |
| Azure Web App (B1) | ~¥2,000 | Linux, 1コア, 1.75GB |
| Cosmos DB (Serverless) | ~¥1,000 | RU消費量依孁E|
| Key Vault (Standard) | ~¥500 | |
| Application Insights | ~¥500 | |
| Azure Bot Service (F0) | ¥0 | Free |
| **合訁E* | **~¥16,000/朁E* | |

---

## 付録C: 参老E��ンク

| リソース | URL |
|---------|-----|
| Azure Bot Service�E�Eingle-Tenant作�E�E�E| https://learn.microsoft.com/en-us/azure/bot-service/abs-quickstart |
| Teams SDK�E�旧Teams AI Library�E�E| https://github.com/microsoft/teams-sdk |
| M365 Agents SDK | https://github.com/microsoft/Agents |
| M365 Agents Toolkit�E�旧Teams Toolkit�E�E| https://github.com/OfficeDev/microsoft-365-agents-toolkit |
| M365 Agents Toolkit VS Code拡張機�E | https://marketplace.visualstudio.com/items?itemName=TeamsDevApp.ms-teams-vscode-extension |
| Agents Toolkit v5 ガイド！Eiki�E�E| https://github.com/OfficeDev/microsoft-365-agents-toolkit/wiki/The-Microsoft-365-Agents-Toolkit-Visual-Studio-Code-v5-Guide |
| Toolkit Bot MSI認証への移衁E| https://learn.microsoft.com/en-us/microsoftteams/platform/toolkit/update-bot-me-app-to-use-certificate-or-msi-for-authentication |
| Azure Bot MI作�EガイチE| https://learn.microsoft.com/en-us/microsoft-365/agents-sdk/azure-bot-create-managed-identity |
| AI Search Integrated Vectorization | https://learn.microsoft.com/en-us/azure/search/vector-search-integrated-vectorization |
| AI Search Cosmos DB Indexer | https://learn.microsoft.com/en-us/azure/search/search-how-to-index-cosmosdb-sql |
| AI Search Managed Identity設宁E| https://learn.microsoft.com/en-us/azure/search/search-how-to-managed-identities |
| Semantic Ranker有効匁E| https://learn.microsoft.com/en-us/azure/search/semantic-how-to-enable-disable |
| Cosmos DB RBAC設宁E| https://learn.microsoft.com/en-us/azure/cosmos-db/nosql/how-to-connect-role-based-access-control |
| Adaptive Card スクロールコンチE�� | https://learn.microsoft.com/en-us/microsoftteams/platform/task-modules-and-cards/cards/cards-format |
