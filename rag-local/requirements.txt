# データ処理
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.2
xlsxwriter>=3.1.0

# LangChain（LLM拡張検索機能用）
langchain>=0.1.0
langchain-google-genai>=4.0.0

# Google Cloud / Vertex AI（LLM + 埋め込みモデル）
langchain-google-vertexai>=3.2.0
google-cloud-aiplatform[vertexai]>=1.35.0
google-auth>=2.17.0

# Azure（埋め込みモデル + Key Vault認証）
openai>=1.0.0                          # Azure OpenAI Embedding API用（LLM用ではない）
azure-identity>=1.15.0
azure-keyvault-secrets>=4.7.0  # Key Vault経由のGCP認証（オプション）

# ベクトルDB
chromadb>=1.0.15

# 日本語形態素解析
sudachipy>=0.6.8
sudachidict-core>=20230927

# UI
streamlit>=1.30.0

# ユーティリティ
python-dotenv>=1.0.0
PyYAML>=6.0
tenacity>=8.0.0
tqdm
rich>=13.0.0